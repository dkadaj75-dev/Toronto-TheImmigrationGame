# Condo Life — Web Edition: Project Context Addendum

**Status date**: 2026-07-14 (updated with §7 locked designs: doors-as-assets, facing vector, accidents, tool navigation). Where this file and the roadmap disagree, **this file wins** — it records design decisions made after the roadmap was written.

---

## 1. The overriding goal: full designer autonomy, zero code

The designer (Septentrion) must be able to build and balance the entire game **through the browser tool suite, without AI or coding in the loop**. Concretely, the tools must eventually cover:

| Capability | Tool | Status |
|---|---|---|
| Add / remove / edit **assets** (prices, footprint, mesh, interactions, gains) | Asset Editor | ✅ done |
| Balance **every gameplay parameter** (decay rates, gains, thresholds, time scale…) | Tuning Editor | ✅ done |
| Rearrange furniture on a map, bake nav | Map Editor slice 1 | ✅ done |
| Map **character animation clips to game states** + rig settings | **Animation Mapper** (accepts `.fbx` animation sources too, §2 addendum, 2026-07-14) | ✅ done (2026-07-13) |
| **Create / edit maps**: draw floors, walls, doors; multiple maps; floor-plan tracing underlay | Map Editor slice 2 | ✅ done (2026-07-13) |
| **Add / remove needs and skills** (not just edit existing ones) | Tuning Editor extension | ✅ done (2026-07-13) |
| **Add / remove actions** and per-action gains, animation state, autonomy eligibility | Interaction Editor | ✅ done (2026-07-13) |
| **Quest system**: author quests with trigger & completion conditions + rewards | Quest Editor (replaces roadmap's "Story Editor") | ✅ done (2026-07-13, see §3.3–§3.4) |
| Simulate balance headlessly, chart curves | Balance Dashboard | ⬜ nice-to-have |
| **Navigate between all admin tools** (tabs/buttons in every tool) | Shared tool nav bar | ✅ done (2026-07-14) |
| **Asset facing direction** ("normal" vector: where the object faces, for use-spots/seating logic) | Asset Editor + Map Editor arrow | ✅ done (2026-07-14, see §7.2 as-built) |
| **Doors as their own asset type** (mesh, hinge axis, scale fit, open/close timing) | Asset Editor door section + Map Editor door↔asset link | ✅ done (2026-07-14, see §7.1 as-built) |
| **Accidents** (per-asset risk config; accident assets that override base interactions) | Asset Editor risk section (accident types are assets) | ✅ done (2026-07-14, see §7.3 as-built) |
| **Buy/Sell mode** (player buys/moves/sells furniture in-game; per-asset icons + quest-gating) | Asset Editor icon/requiresQuestUnlock fields + in-game Buy Mode HUD | ✅ done (2026-07-14, see §7.6 as-built) |
| **Character overhead marker** (Sims plumbob — GLB/image/built-in default, spin/bob, sim time) | Animation Mapper | ✅ done (2026-07-14, see §7.7 as-built) |

Data-driven rule of thumb for every new feature: *if the designer might want to change it, it's a JSON field with a tool UI, not code.*

---

## 2. Character & animation system (as built)

- `game/anim.ts` — `AnimController` over a `THREE.AnimationMixer`. Logical states (`idle`, `walk`, `sit`, `lie`, or any action's `animation` field value) resolve to clip names through `tuning.character.clipMap` (exact → case-insensitive → substring). Missing clips warn once and fall back to idle. Cross-fade duration, walk-clip playback rate (`walkSpeed / walkClipSpeedReference`), sit/lie perch heights — all in `tuning.character`.
- `game/world.ts` — `loadRiggedCharacter()` loads `tuning.character.meshPath`, **merges clips from every GLB in `tuning.character.animationPaths`** (Mixamo-style one-clip-per-file exports; same skeleton required), normalizes to `heightMeters` (centered, grounded), optional `yawOffsetDeg` wrapper for models not facing +Z. Load failure keeps the capsule stand-in.
- `game/sim.ts` — `onLocomotionChange` callback drives idle↔walk; `hasRig` flag disables the capsule-era tip-over lie hack; perch heights from tuning.
- `game/main.ts` — mixer advances on **sim time** (pause freezes, 2×/3× speeds up). Hot-reload re-tunes the controller and re-loads the rig when `meshPath` or `animationPaths` change.
- **`tools/animations.html` — Animation Mapper**: lists the model's actual clips, previews any clip on the character in 3D, maps clips to states via dropdowns, manages animation source GLBs, and edits all rig numbers. States shown = core (`idle/walk/sit/lie`) + every `animation` value referenced by actions + designer-added custom states. Unmapped states flagged ⚠. Saves the whole `tuning.json` (character block edited, other groups preserved).
- **T-pose diagnosis**: T-pose = no clip resolved. Open the Animation Mapper — if the clip list is empty, the animations are in separate files: add them under *Animation sources*. Otherwise the clipMap names just don't match: remap from the listed clips.
- **FBX animation sources (2026-07-14 addendum)**: `tuning.character.animationPaths` entries ending in `.fbx` (Mixamo-style: character body is one GLB, each animation is its own separate FBX) are loaded via `three/addons/loaders/FBXLoader.js` instead of GLTFLoader — `game/world.ts`'s `loadFbxClips`/`isFbxPath`/`collectBoneNames` do this, called from both `loadRiggedCharacter`'s clip-merge loop AND `tools/animations.html`'s preview loader (one implementation, per §5's never-reimplement rule). `.glb`/`.gltf` sources are completely untouched — same code path as before, byte-identical behavior when no `.fbx` path is configured. Mixamo-FBX quirks, all handled in the pure/headless-tested `game/fbxclips.ts` (`test/fbxclips.test.ts`, `npx tsx`):
  - **Bone retargeting**: each clip track is resolved against the base rig's actual bone names — exact match passes through unchanged, else the `mixamorig:`/`mixamorig` prefix is stripped and re-checked, else the track is left as-is and logged in a single one-time console warning (`retargetTrackName`/`collectBoneNames`).
  - **Scale / root motion — decision: strip, no tuning flag**. Mixamo FBX is authored in centimeters (root-position tracks ~100x off), but rather than detect-and-rescale "the root bone," ALL position tracks are dropped outright (`stripPositionTracks`) — Mixamo skeletons only ever keyframe position on the root/hip bone in the first place (everything else is rotation-only), and the game's locomotion is already procedural (`anim.ts` scales the walk clip's `timeScale` off actual ground speed, never root motion), so an in-place idle/walk/sit/lie loop has zero use for baked translation regardless of its scale. Judged unambiguous enough to not need a `tuning.character.fbxRootTranslation` flag.
  - **Clip naming**: Mixamo exports every clip embedded-named `"mixamo.com"` (or blank). `resolveClipName` falls back to the source file's basename (no extension) whenever the embedded name is missing or already used by an earlier-merged clip, with an incrementing `_2`/`_3`… suffix if even that collides — keeps the Animation Mapper's per-state clip dropdown unambiguous with multiple FBX sources.
  - Actual FBX parsing (`FBXLoader`) is browser-only, not headless-testable (jsdom can't run it) — verified instead by loading the game/tool in-browser; `game/fbxclips.ts`'s string-level decisions are what's unit-tested.
  - **Designer instructions**: drop `.fbx` files anywhere under `public/` (e.g. `public/models/anims/`), then in the Animation Mapper's *Animation sources* card add each path (e.g. `/models/anims/walking.fbx`) the same way as a GLB source — the clip(s) inside appear in *Clips in the model* and can be previewed/mapped exactly like GLB-sourced clips. No other setup needed; retargeting/scale/naming are automatic.
- The user's desired state vocabulary (idle, walking, running, sitting, laughing, talking, screaming, cooking, sleeping, reading…): `idle`/`walk` are automatic; everything else becomes live when an **action**'s `animation` field references it. "Running" would need a game-side locomotion feature (e.g. run-when-far) — not built; treat as a future tuning flag.

## 2b. Maps & the active-map mechanism (as built, Map Editor slice 2)

- **`tools/map.html`** is now the complete Map Editor (slices 1+2 in one file — note: the user's tools folder was missing the slice-1 map.html, so this file is standalone and authoritative). Modes: **Objects** (palette add, drag with ½-cell snap, R rotate with 90° normalization, inspector x/z/rot), **Floors** (drag to draw rectangle floors with a chosen material — compose several rects for L-shaped rooms; click to edit id/material/delete), **Walls** (drag to draw, axis-locked and snapped; click to edit endpoints numerically or delete), **Doors** (click a wall to place — position projects onto the wall and snaps along it, orientation inferred from wall axis; drag to move, R flips orientation), **Spawn** (click to place, facing° in inspector). Plus: **Ctrl+Z undo** (50-step snapshot stack, closes a known gap), floor-plan **underlay** image with opacity/width/offset controls (session-only tracing guide, not saved), nav overlay (game's own `nav.ts` via Vite, rebaked lazily on mutation), map-properties editing (name, gridSize, bounds).
- **Multiple maps**: header select + **New / Duplicate / Delete** (new & duplicated maps are PUT to `data/maps/<id>.json` immediately). `server.js` gained `GET /api/maps` (listing) and `DELETE /api/data/maps/*.json` (deletion is refused for non-map files). Deleting the active map or the last map is blocked in the tool.
- **Active map**: `tuning.map.active` names which `data/maps/<id>.json` the game plays (default `condo`). `data.ts` loads tuning first, then that map. The editor's **"▶ Play this map"** button PUTs tuning with the current map id — the running game hot-reloads into it, and `main.ts` detects the map-id change and teleports the sim to the new spawn (`SimAgent.teleportTo`).

## 2c. Needs & skills add/remove (as built, Tuning Editor extension, 2026-07-13)

- **No new tool file** — `tools/tuning.html`'s existing Needs/Skills sections already render one editable group per entry (`renderNeeds`/`renderSkills`), so add/remove was a natural extension rather than a new Stats Editor page. A dedicated `tools/stats.html` would have duplicated that machinery for no benefit; extending was the cleaner call here (contrast with the Map Editor, which got its own file because it needed a canvas + very different interaction model).
- **Add**: "+ Add need" / "+ Add skill" toolbar buttons (`[data-action="add-need"|"add-skill"]`) at the top of each section. `window.prompt` asks for a display **name only**; the id is derived (`slugify` → lowercase, non-alphanumerics → `_`) and uniquified against the existing list (`thirst`, `thirst_2`, …) — no separate id prompt, no failure path to retry. New-entry defaults are **averaged from existing (non-computed) entries**, not hardcoded: a need copies the mean `default`/`decayPerTick` of current needs and starts `autonomy: true`; a skill starts at `default: 0` (skills grow through practice, never decay — no `decayPerTick` field exists on `SkillDef`) with `max` averaged from existing skills. Bar `color` is randomized (designer edits it via the existing color field same as any entry).
- **Remove**: per-entry "Delete need"/"Delete skill" buttons (`[data-action="delete-need"|"delete-skill"][data-need-id|data-skill-id]`). Referential integrity is checked against `interactions.json`: a need can be referenced by an action's `needGains` key or its `primaryNeed`; a skill only by `skillGains`. **Chosen UX (deliberately different from the Asset Editor's "warn and leave dangling" pattern)**: the confirm dialog lists every referencing action by name and field, and on confirm the tool **strips the dangling references** (deletes the `needGains`/`skillGains` key, nulls a matching `primaryNeed`) in the same save cycle — `interactions.json` is marked dirty alongside `stats.json` so both PUT together. Rationale: `game/stats.ts`/`game/autonomy.ts` already no-op safely on an unknown id (nothing crashes either way), but shipping a `needGains`/`skillGains` key that points at nothing is exactly the kind of orphaned data this data-driven design is supposed to avoid, and stripping is a single confirm click away from being wrong-footed by a docs/CLAUDE.md-style comment describing a field that no longer means anything.
- **Ordering gotcha (documented in code comments)**: `render()` fully rebuilds section DOM (`innerHTML = ''` + re-append), which discards any `.dirty` class applied beforehand — `markDirty(file)` must be called **after** `render()` in the add/remove handlers, not before, or the dirty indicator (and the Save button's enabled state) silently fails to reflect the change. (Plain field edits don't hit this because they mutate in place without re-rendering.)
- **No hardcoded ids found in game code**: audited `game/*.ts` for literal need/skill strings (`hunger`, `energy`, `english`, etc.) — none exist. `game/stats.ts` (`SimStats`), `game/ui.ts` (`Hud.rebuildBars`), and `game/autonomy.ts` all iterate `stats.needDefs`/`skillDefs`/`lowestAutonomyNeed()` purely by whatever ids are in `stats.json`; the Environment "computed" need is found dynamically (`data.stats.needs.find(n => n.computed)` in `game/main.ts`), not by id. Confirmed live: added/removed needs and skills render on the in-game HUD and drive decay/autonomy with zero code changes.
- **Tests**: `tools/tuning-editor.test.mjs` extended (not a new suite) with add/cancel/uniquify coverage for both needs and skills, and both referential-integrity branches (unreferenced → plain message; referenced → lists the action(s), strips the reference on confirm, cancel leaves everything untouched), plus a final PUT assertion proving `stats.json` and `interactions.json` save the fully-reconciled state together.
- **Environment note**: this session also discovered the repo's `node_modules` had never actually been installed (no `jsdom`/`tsx`/`canvas`) — every existing test suite would have failed to even start. Added `jsdom`, `tsx`, and `canvas` as devDependencies so the full suite (tool `.mjs` + game `.ts`) is runnable; this is a one-time environment fix, not a code change.

## 2d. Interaction Editor (as built, 2026-07-13)

- **New tool file `tools/interactions.html`** — sidebar (all actions, "player-only" badge when `autonomyEligible` is false) + editor panel, following the Asset Editor's layout (contrast with §2c's needs/skills, which extended an existing section instead — actions have enough fields and their own referential-integrity direction that a dedicated page was the cleaner call here, same reasoning as why Map Editor got its own file).
- **Autonomy-eligibility flag: pre-existing, not added.** `data/interactions.json`'s `ActionDef` already carried `autonomyEligible: boolean` on every action (`game/data.ts` `ActionDef` interface, line ~13), and `game/autonomy.ts`'s `maybeAct()` already gates candidates on it (`if (!action || !action.autonomyEligible) continue;`) before matching `primaryNeed`. No schema change and no `autonomy.ts` change were needed — the editor just exposes the existing flag as a checkbox, so e.g. "Wash hands" or "Cook" (already `autonomyEligible: false` in the shipped data) stay player-only, and toggling it live-round-trips through the sim's autonomy search on save.
- **Editable per action**: name, free-text `animation` state (datalist + hint line listing every state already in use — the 4 core states `idle`/`walk`/`sit`/`lie` the Animation Mapper always recognizes, plus every other action's current `animation` value — so the designer isn't guessing a name that already drives a mapped clip), `autonomyEligible`, `seatAware` (sparse — key only present when true, mirroring the Asset Editor's `seatTarget` convention), `primaryNeed` (dropdown fed from `stats.json` needs + "(none)", writes `null` not a missing key — matches the schema's `primaryNeed: string | null`), and one numeric row per need/skill in `stats.json` for `needGains`/`skillGains` (blank = no entry in the map) — same "fed from data, no free-typed ids" pattern as the Tuning Editor's per-action gain rows, just given a dedicated full-field page instead of a compact grid.
- **id is fixed** once created (assets reference it by string, like an asset's own id) — editing swaps the display name only.
- **Add**: "+ New action" prompts for a display name only; id = `slugify(name)` uniquified against existing action ids (`do_yoga`, `do_yoga_2`, …) — identical derivation helper to the Tuning Editor's `addNeed`/`addSkill`. Defaults: `needGains: {}`, `skillGains: {}`, `animation: ''`, `autonomyEligible: true`, `primaryNeed: null` (sane no-ops until the designer fills them in).
- **Referential integrity** is checked against `data/assets.json` (an asset's `interactions: string[]` array). Delete follows the same "warn with names, strip on confirm" policy as §2c: unreferenced action → plain confirm; referenced action → confirm dialog lists every referencing asset by name, and on confirm the tool removes the id from those assets' `interactions` arrays and deletes the action, marking **both** `interactions.json` and `assets.json` dirty so they save together in one PUT cycle — `assets.json` never ships pointing at an action id that no longer exists.
- **Hot-reload**: works with zero changes. `game/data.ts`'s `watchData()` already polls and reloads the whole `GameData` bundle (including `interactions.json`) every 2s via `loadAll()`, and `game/main.ts`'s hot-reload callback does `data = fresh` (a `let`, not `const`) — every consumer that reads `data.interactions.actions` at use-time (e.g. building the action menu, `WatchTick`-equivalent gain application, `autonomy.ts`'s `actionsById` map) picks up edited/added/removed actions on the next poll with no extra plumbing.
- **Tests**: new suite `test/interaction-editor.test.mjs` (placed alongside the other newest suites, `test/map-editor.test.mjs` and `test/animation-editor.test.mjs`, rather than `tools/` where the two oldest suites still live) covers render + player-only badge, editing every field including the sparse `seatAware` key and the `primaryNeed` null round-trip, add with slugify+uniquify, both delete branches (unreferenced plain-confirm; referenced confirm-lists-assets-and-strips, asserting the exact stripped `assets.json` PUT payload), and search filtering.

## 3. Quest system — locked design (build in a future slice)

Replaces the roadmap's vaguer "Story Editor". Two parts:

### 3.1 Sim-state variables (`data/simstate.json` or a `variables` block)
Designer-defined variables beyond needs/skills, e.g. `visaStatus` (string), `job` (string|null), `income` (number), plus the built-in `funds`. Defaults live in data; the save system persists current values. Variables are readable by quest conditions and writable by quest rewards. This also finally gives `tuning.economy.startingFunds` a consumer.

### 3.2 Quests (`data/quests.json`) — draft schema
```json
{
  "quests": [{
    "id": "first_words",
    "name": "First Words",
    "description": "Reach English level 10 and save §500.",
    "trigger":    { "all": [ { "var": "skills.english", "gte": 1 } ] },
    "completion": { "all": [ { "var": "skills.english", "gte": 10 },
                             { "var": "funds", "gte": 500 } ] },
    "rewards": [ { "type": "funds", "amount": 200 },
                 { "type": "setVar", "var": "visaStatus", "value": "student" } ],
    "onceOnly": true
  }]
}
```
- **Condition namespace**: `needs.<id>`, `skills.<id>`, `funds`, `time.hour`, `time.day`, `vars.<name>`, `quests.<id>.state` (`locked|active|done`).
- **Operators**: `gte`, `lte`, `eq`, `neq`; combinators `all` / `any`, nestable.
- **Reward types**: `funds`, `setVar`, `unlockAsset` (hides/shows catalog entries — ties into Buy/Sell mode), later `dialogue` / vocabulary popups for the language-learning content.
- **Evaluation**: on the needs-decay tick (same reuse-an-existing-interval convention as autonomy). Quest state persists in the save.
- **Quest Editor tool**: CRUD quests, condition builder with dropdowns fed by `stats.json`/variables (no free-typed ids), live validation, and a quest log HUD panel in-game (trigger → "quest started" toast; completion → rewards applied + toast).

Build order within the quest slice: 1) variables + condition evaluator (headless-tested — this is pure logic, perfect for the test suite), 2) in-game quest runner + HUD log, 3) Quest Editor tool.

## 3.3 Quest system runtime — as built (steps 1–2, 2026-07-13)

Steps 1–2 of §3's build order are shipped (game side only — no Quest Editor tool yet, that's step 3).

- **New data files**: `data/simstate.json` (designer variable defaults — `visaStatus` string, `job` string|null, `income` number, each `{ id, name, type, default }`; `funds` is deliberately NOT in this file, see below) and `data/quests.json` (shipping the exact `first_words` example from §3.2, ids matched to the real `stats.json`: `skills.english` — English's `max` is 10, matching the quest's `gte: 10` completion threshold). Both are generic `data/*.json` files; `server.js`'s `/api/data/<path>` and `/data/<path>` routes already serve/accept any `.json` under `data/` with no code change needed — verified by loading both through the existing `GET/PUT` handlers.
- **New module `game/quests.ts`** (pure, no DOM/three.js — headless-testable):
  - `evaluate(condition, ctx)` — recursive evaluator for the §3.2 condition tree (`all`/`any`, `gte`/`lte`/`eq`/`neq`) against an `EvalContext` snapshot (`needs`, `skills`, `funds`, `time.{hour,day}`, `vars`, `quests` — all plain records, built fresh each tick by the caller).
  - **Unknown-id semantics (decision)**: any `var` path that doesn't resolve (unknown need/skill/var/quest id, or an unrecognized namespace) resolves to `undefined`, and every operator against `undefined` is `false` — never throws. A leaf with none of `gte/lte/eq/neq` set is likewise `false`. `all([])` is vacuously `true`; `any([])` is `false` (nothing to satisfy) — both just `Array.prototype.every`/`some`'s native behavior, called out explicitly in the code comment as intentional.
  - `QuestRunner` — the runtime state machine: `quests: Record<id, 'locked'|'active'|'done'>`, `vars: Record<id, value>`, `funds: number`, `unlockedAssets: Set<string>`, `completedLog: string[]`. `tick(needs, skills, time)` evaluates all `locked` quests' triggers (→ `active`, fires `onQuestStarted`), then all `active` quests' completions (applies rewards, fires `onQuestCompleted`) in the same pass — a quest can in principle trigger and complete in the same tick if its completion condition already holds; documented as intentional rather than special-cased, since §3.2 doesn't require a one-tick delay.
  - **onceOnly semantics**: `onceOnly: true` quests go `active → done` and stay `done` forever (rewards apply exactly once). `onceOnly: false` quests go `active → done` (rewards apply) then **immediately reset to `locked`**, so they can trigger again later — this is the "repeatable quest" reading of §3.2's "onceOnly:true quests never re-trigger" (which implies the `false` case can). Not explicitly speced beyond that phrase; flagged here as a design call for the Quest Editor agent to know about (a repeatable quest's rewards fire every time it completes).
  - **Rewards**: `funds` (add to balance), `setVar` (write `vars[name] = value`), `unlockAsset` (add an asset id to `unlockedAssets`).
  - **unlockAsset / Buy-Sell tie-in**: implemented as an additive `Set<string>` of asset ids a quest has explicitly unlocked (`isAssetUnlocked(id)` helper), not a schema change to `assets.json` — no asset currently opts into being quest-gated. Documented expectation for the future Buy/Sell mode (roadmap §4 item 6): an asset is purchasable if it has no quest-gate flag OR `isAssetUnlocked` is true; that gate flag itself (e.g. a future `AssetDef.requiresQuestUnlock`) is for that slice to add.
  - **Hot-reload (`retune`)**: adopts new/edited quest and variable *definitions* without touching runtime *state* — existing quest states and variable values are preserved; only brand-new quest ids get `locked` and brand-new variable ids get their default. Removed ids leave an orphaned (harmless) runtime entry, mirroring `stats.ts`'s `retune()` convention.
  - **Persistence — deferred**: there is no save system in this repo (`data/save/` is empty and gitignored; no `save.ts` module exists anywhere). Quest/variable/funds state is therefore **runtime-only** (resets on page reload), same as needs/skills today. `QuestRunner.serialize()`/`restore(QuestSaveState)` produce/consume a flat JSON-serializable shape specifically so wiring a future save system is a direct `JSON.stringify(runner.serialize())` / `runner.restore(parsed)` call, no refactor needed.
- **`game/data.ts`** gained `VarDef`/`SimStateData`, `Condition`/`ConditionLeaf`/`ConditionAll`/`ConditionAny`, `QuestState`, `Reward`/`RewardFunds`/`RewardSetVar`/`RewardUnlockAsset`, `QuestDef`/`QuestsData` types; `GameData` gained `simstate`/`quests`; `TuningData` gained `quests: { toastDurationSeconds, completedLogLimit }` (both now in `data/tuning.json`, no hardcoded numbers); `loadAll()` fetches the two new files alongside the rest.
- **`game/main.ts`** wiring: `QuestRunner` constructed after `stats`/`hud`/`autonomy`, seeded with `data.tuning.economy.startingFunds` (giving that field its first real consumer, per §3.1). `quests.tick(...)` is called **inside the existing needs-decay `while` loop** in `simTick` (no new accumulator/timer — reuses `decayAcc`/`needsDecayTickSeconds`, per §3.2's "same reuse-an-existing-interval convention"). Needs/skills are snapshotted per-tick via `Object.fromEntries(stats.needs/.skills)`. `time.day` is new: the render loop's clock advance changed from `gameSeconds = (gameSeconds + …) % 86400` to an explicit `while (gameSeconds >= 86400) { gameSeconds -= 86400; gameDay++; }`, so `time.day` (1-indexed, starts at 1) is now tracked and available to conditions. Quest start/complete callbacks trigger HUD toasts and rebuild the quest log; hot-reload calls `quests.retune(...)` + rebuilds the log.
- **HUD (`game/ui.ts`)**: new collapsible `#quest-panel` (bottom-left, above the devbar, same visual language as the needs/skills panels) listing active quests (name + description) and a "Completed" section (last `tuning.quests.completedLogLimit` entries, newest first). New `#quest-toasts` stack (top-center, non-interactive/`pointer-events:none` so it never blocks taps) shows transient "Quest started: X" / "Quest completed: X" toasts, auto-dismissing after `tuning.quests.toastDurationSeconds`. `Hud.setQuestLog(active, completed, limit)` and `Hud.showQuestToast(text, kind, durationMs)` are the new public methods.
- **Hot-reload semantics (documented behavior)**: `quests.json`/`simstate.json` flow through the existing `watchData()`/`loadAll()` 2s poll exactly like every other data file — editing a quest's name/description/conditions/rewards on disk live-updates the definition, but a quest already `active` or `done` stays that way (only its displayed name/description/conditions change); a brand-new quest in the file starts `locked` and evaluates normally from then on.
- **Tests**: new `test/quests.test.ts` (`npx tsx test/quests.test.ts`) — operators (all 4), every namespace including `quests.<id>.state`, `all`/`any` nesting (incl. empty-array vacuous cases), unknown-id/malformed-leaf → false for every namespace, full runner lifecycle (locked→active→done, onceOnly true and false/repeatable, all 3 reward types, `retune` preserving state while adopting new defs, `serialize`/`restore` round-trip). All existing suites re-run clean: `anim.test.ts`, `charfit.test.ts`, `nav.test.ts`, `locomotion.smoke.ts` (via `npx tsx`), `animation-editor.test.mjs`/`interaction-editor.test.mjs`/`map-editor.test.mjs` (via `node`, under `test/`), `asset-editor.test.mjs`/`tuning-editor.test.mjs` (via `node`, under `tools/`). **Pre-existing quirk noted, not introduced by this slice**: `test/meshfit.test.mjs` fails under plain `node` (`ERR_MODULE_NOT_FOUND` — it imports `../game/world` without an extension, which only Node's ESM-extensionless resolution under `tsx` handles) but passes under `npx tsx test/meshfit.test.mjs`; left as-is since it's unrelated to this slice. Strict type gate (`npx tsc --noEmit --strict --target es2020 --moduleResolution bundler --module esnext --skipLibCheck game/*.ts`) is clean.

## 3.4 Quest Editor tool (as built, 2026-07-13)

Step 3 of §3's build order — the last piece of the quest slice. **New tool file `tools/quests.html`**, no game-module imports needed (pure JSON editing, so it's a plain inline script only, same as `tools/tuning.html` — no `type="module"` section, contrast with the Map Editor / Animation Mapper which genuinely need `nav.ts`/`world.ts`).

- **Layout**: one page, two `<section>`s stacked (mirrors `tools/tuning.html`'s multi-section single-column layout, not `tools/interactions.html`'s single sidebar+editor, since this tool has two distinct entity kinds). **Variables** (`data/simstate.json`): a toolbar (`+ Add variable`) + one row per `VarDef` — name, id (fixed after creation), a `type` dropdown (`string`/`number`/`boolean`, matching the real `VarDef.type` in `game/data.ts` — not the draft `string|null` phrasing in the slice brief, which the shipped type superseded), and a default-value control with a separate "null" checkbox (needed because `VarDef.default` can be `string | number | boolean | null` independent of `type` — e.g. the shipped `job` variable is `type: 'string'` with `default: null`). Changing `type` coerces any non-null default to a sane value of the new type. **Quests** (`data/quests.json`): a sidebar (name + a "repeatable" badge when `onceOnly` is false) + an editor panel, same visual pattern as `tools/interactions.html`.
- **Condition builder**: recursive renderer over the `Condition` tree (`isLeafCond`/`renderGroupBox`/`renderLeafRow`). Every rendered node carries a `data-cond-path` positional address (`"trigger"`, `"trigger.0"`, `"trigger.1.0"`, …) computed from array indices at render time; every structural edit (add leaf, add group, remove, change combinator, change a leaf's var) triggers a full `renderQuestEditor()` re-render (paths are recomputed fresh each time — simpler and more robust than trying to preserve DOM identity across array-length changes, at the cost of losing focus on structural edits; plain value typing does **not** re-render, so text/number entry keeps focus). Each group box has an All/Any `<select>` (swapping the object's `all`/`any` key while keeping the same child array) plus inline hint text stating the empty-group semantics; each leaf is a var-path `<select>` (dropdown-only, **never free-typed** — options are grouped by namespace: Needs/Skills from `stats.json`, Economy (`funds`), Time (`time.hour`/`time.day`), Variables from `simstate.json`, Quests (`quests.<id>.state`, self-reference allowed per the brief) + an operator `<select>` (`gte`/`lte`/`eq`/`neq`) + an adaptive value input resolved by `describeVarPath()`: number input for needs/skills/funds/time, a `locked`/`active`/`done` `<select>` for quest-state paths, and for `vars.<name>` paths a control typed by that variable's own `type` (checkbox-as-select for boolean, number input for number, text otherwise). A stale/hand-edited var path that doesn't match any live option still renders (in an "Unknown (stale id)" `<optgroup>`) rather than silently snapping to a different value. Changing a leaf's var or operator resets its value to a type-appropriate default (`defaultValueFor`), never leaving a type-mismatched leftover (e.g. a string value under `gte`).
- **Rewards builder**: one row per `Reward`, a type `<select>` (`funds`/`setVar`/`unlockAsset`) that swaps in a fresh default reward object on type change, then type-specific fields — `funds`: amount number input; `setVar`: variable dropdown (from `simstate.json`) + adaptive value input (same typing rule as condition leaves); `unlockAsset`: asset dropdown (from `assets.json`). Add/remove rows via `+ Add reward` / a per-row `× remove`.
- **Add quest**: name prompt → `slugify` + `uniqueId` (identical helper pattern to every prior editor) → `{ trigger: {all:[]}, completion: {all:[]}, rewards: [], onceOnly: true }`. The empty-`all` vacuous-TRUE-on-both-trigger-and-completion warning isn't a separate dialog — it shows up immediately in the inline group hint text *and* the validation panel, which is arguably more useful (visible for the life of the quest, not just at creation).
- **Referential integrity — both directions, same "warn with names, strip on confirm" policy as §2c/§2d**:
  - **Delete a variable**: scans every quest's `trigger`/`completion` trees for a leaf with `var === "vars.<id>"` (via a generic `anyLeaf`/`stripCondition` recursive walker reused for both variables and quests) and every `rewards` array for a `setVar` entry with that `var`. Confirm dialog lists referencing quests by name and which part references it; on confirm, condition leaves are stripped (the containing `all`/`any` array is kept, even if now empty — an emptied `all` becomes vacuously TRUE, an emptied `any` becomes FALSE, both stated in the dialog text) and matching `setVar` rewards are removed outright (there's no meaningful "null" reward to leave behind). Both `simstate.json` and `quests.json` are marked dirty and saved together.
  - **Delete a quest**: scans every *other* quest's conditions for a leaf `var === "quests.<id>.state"` (self-references are irrelevant since the quest and its own leaf disappear together). Same strip-on-confirm behavior, `quests.json` only (a quest reference never touches `simstate.json`).
  - `stripCondition(node, predicate)` mutates combinator arrays in place, recursing through arbitrarily nested `all`/`any`; it returns `null` only if the *root* condition is itself a stripped leaf (bare-leaf root is legal per the `Condition` union though the editor never produces one), and callers fall back to `{ all: [] }` in that case.
- **Validation panel**: recomputed on every render (`collectValidationIssues` → `renderValidation`), covers (a) empty `all`/`any` groups with their exact runtime semantics spelled out, (b) unknown ids for every namespace (`needs.`/`skills.`/`vars.`/`quests.<id>.state`) plus unrecognized namespaces entirely, (c) a leaf with none of `gte`/`lte`/`eq`/`neq` set, (d) reward-side unknown ids (`setVar`'s var, `unlockAsset`'s asset) and a non-numeric `funds` amount. **Decision: validation never blocks saving** — the header text says so explicitly ("warnings only, saving is never blocked"), because a designer mid-edit (e.g. a quest with an intentionally empty group while wiring up its conditions) shouldn't be locked out of saving their in-progress work; the dropdowns already prevent *new* unknown ids from being created; unknown ids only show up from hand-edited JSON or an id deleted elsewhere, and the panel's job is to surface that, not gate it.
- **Tests**: new `test/quest-editor.test.mjs` (modeled on `test/interaction-editor.test.mjs`) — render (variable rows, quest sidebar incl. repeatable badge), variable CRUD (type/default edits incl. the null-default toggle, add+uniquify, both delete branches with exact stripped PUT payloads spanning both files), quest add+uniquify, a full condition-builder sequence building a nested `{all:[{var:funds,eq:500},{any:[{var:skills.english,gte:5}]}]}` tree and asserting the *exact* resulting JSON, leaf removal, all 3 reward types (add/edit/remove) with exact PUT payloads, quest delete both branches (referenced — strips the other quest's condition leaf; unreferenced — plain confirm), validation-panel assertions (unknown need/variable ids, missing operator, emptied group semantics), and sidebar search filtering. All pass; the full existing suite (`test/*.mjs`+`tools/*.mjs` via `node`, `test/*.ts`+`test/meshfit.test.mjs` via `npx tsx`) re-runs clean — no regressions. `game/*.ts` untouched, so the strict type gate wasn't re-run (nothing to gate).

## 4. Updated build priorities (supersedes roadmap §7 ordering)

1. ~~Animation Mapper + multi-file clips~~ ✅ shipped 2026-07-13
2. ~~Map Editor slice 2~~ ✅ shipped 2026-07-13
3. ~~Needs & skills add/remove in the Tuning Editor~~ ✅ shipped 2026-07-13 (see §2c below)
4. ~~Interaction Editor~~ ✅ shipped 2026-07-13 (see §2d) — add/remove actions, per-action gains, animation state, autonomy flags (unlocks the full animation vocabulary: cook, read, scream…)
5. ~~Quest system per §3 (variables → runner → editor)~~ ✅ shipped 2026-07-13 — all 3 steps done: variables + evaluator, runner + HUD log (§3.3), Quest Editor tool (§3.4)
6. ~~Shared tool nav bar~~ ✅ shipped 2026-07-14 (§7.4) — quick win, unblocks nothing but improves every tool session
7. ~~Asset facing vector~~ ✅ shipped 2026-07-14 (see §7.2 as-built) — foundational: use-spots and seat-facing logic feed doors' and accidents' placement math, and Buy/Sell placement UX later
8. ~~Doors as assets~~ ✅ shipped 2026-07-14 (see §7.1 as-built) — the door-specific block (`door: {...}`), animated hinge panel, and Map Editor door↔asset link all landed
9. ~~Accidents system (§7.3)~~ ✅ shipped 2026-07-14 (see §7.3 as-built) — risk config, roll, placement, hierarchy, cleanup, tools all landed
10. ~~Buy/Sell mode (Phase 3)~~ ✅ shipped 2026-07-14 (see §7.6 as-built) — designer supplied Sims 1/3/4 Buy Mode screenshots; catalog + placement + move/sell all landed; doors stay `buyable: false` (§7.1, no special-casing needed)
11. ~~Mobile polish / PWA~~ ✅ shipped 2026-07-14 (see §8 as-built) — PWA manifest/service worker/icons, safe-area HUD padding, HUD narrow-width collision fix, dpr cap (was already in place, documented)

## 5. Working conventions with Claude (session bootstrap)

- **Workflow since 2026-07-13: Claude Code works the local repo directly** (reads/edits files, runs tests, commits slice-per-commit on `master`; nothing is pushed without the user asking). The old Claude-Projects ritual (upload zips each session, delivery zips back) is obsolete — kept here only as history. Session bootstrap is now just: read this file + `git log`/`git status`.
- **Slice workflow**: user says "Continue" → Claude proposes/builds the next self-contained slice (usually via a background subagent given this file as spec) → headless tests + strict type gate (`npx tsc --noEmit --strict --target es2020 --moduleResolution bundler --module esnext --skipLibCheck game/*.ts`) → main session verifies and commits → user confirms in-browser.
- **Tests**: TS suites via `npx tsx test/<name>.test.ts`; tool suites via `node test/<name>.test.mjs` (jsdom). Tool tests load the HTML, run the plain inline script (module scripts are inert in jsdom), and drive the DOM.
- **Tool architecture**: editor logic in a plain inline script exposed as `window.<Tool>` (jsdom-testable); three.js and game-module imports isolated in a `type="module"` script (Vite-processed in the browser). Tools import game logic directly (e.g. Map Editor uses `nav.ts`, Animation Mapper uses `normalizeModelToHeight` from `world.ts`) — never reimplement.
- **API**: `GET/PUT /api/data/<path>` on the dev server; tools PUT whole files with `JSON.stringify(data, null, 2)`.
- **Environment**: Windows, project at `D:\WebCreation\condo-life-web`, Node 24/npm 11, `npm run dev` (never `file://`).

## 6. Still outstanding (carried over)

- `CLAUDE.md` exact Unreal numbers — all data-file values remain placeholders until provided.
- Art style lock (Kenney vs Quaternius) — pipeline ready either way.
- Real `secondsPerGameDay` — live-tunable, pick when it matters.
- Known gaps from PROGRESS.md §7 still apply (no Map Editor undo, seat distance cap, single-sim seat occupancy, wall fade cosmetic work). Animated doors shipped 2026-07-14 (§7.1 as-built) — struck from this list.
- The Tuning Editor renders `tuning.json` groups generically — the nested `character.clipMap` object may render awkwardly there; the Animation Mapper is the dedicated surface for that block.

---

## 7. Locked designs — designer requests of 2026-07-14

Four requests from the designer, specced here before implementation (build order in §4 items 6–9). Common thread: everything is data + tool UI per the §1 rule.

### 7.1 Doors as their own asset type

- **Doors become entries in `assets.json`** with `category: "door"` — they reuse the whole asset machinery (mesh, footprint, editor) instead of a parallel system. A new general field `buyable: boolean` (default `true`, absent = true) is added to `AssetDef`; door entries ship `buyable: false` — the designer doesn't want players buying/selling doors *yet*, but flipping one flag changes that later. The future Buy/Sell catalog must filter on `buyable`.
- **Mesh-fit fields, general to ALL assets** (any imported GLB may come mis-scaled/mis-oriented): optional `meshFit: { scale?: number | [x,y,z], yawOffsetDeg?: number, yOffset?: number }` on `AssetDef`, applied by the loader after normalization. This answers the door "resize as required" request but deliberately lands on every asset.
- **Door-specific block** on door-category assets: `door: { hingeOffset: [x, z], openAngleDeg, openSeconds, closeSeconds, triggerDistance }`. `hingeOffset` is the rotation-axis position in model-local meters relative to the model origin — the designer's requested movable Z-rotation axis, so any GLB can be made to swing correctly regardless of where its author put the origin. Per-asset values override tuning defaults `tuning.doors: { openSeconds: 0.5, closeSeconds, openAngleDeg: 90, triggerDistance }` (all live-tunable; 0.5s is the designer's requested default).
- **Map link**: a map's `doors[]` entries gain an optional `assetId` (a door-category asset). With an `assetId` the game renders that asset's mesh on the doorway and animates it; without one, current behavior (bare opening) is unchanged — old maps stay valid. The Map Editor's Doors mode gets a door-asset dropdown (door-category assets only) in its inspector.
- **Open/close logic** (mirrors the Unreal `BP_Door` semantics, which are PIE-proven): each frame-ish (reuse an existing tick, no new timer): if the sim is within `triggerDistance` **and** its current nav path crosses the doorway segment → rotate open around the hinge over `openSeconds`; once the sim is farther than `triggerDistance`, rotate closed over `closeSeconds`. Never close while the sim is within the trigger distance (never trap/clip the sim mid-transit). Standing near a door without a path through it does NOT open it.

### 7.1 as-built (2026-07-14)

- **New module `game/doors.ts`** (pure math, no THREE dependency in its logic half — headless-tested in `test/doors.test.ts`, following the `game/facing.ts` precedent of "dedicated module with pure-testable logic + a thin three.js layer"): `resolveDoorConfig` (sparse per-asset `door` block merged over `tuning.doors`, `null` when the asset has no `door` block), `doorBaseYawDeg`, `hingeWorldPos`/`panelLocalOffset` (hinge transform math), `segmentCrossesDoorway`/`pathCrossesDoorway` (the pair-crossing test), `distanceToDoor`, `doorShouldBeOpen` (the open/close state machine), `stepDoorAngle` (linear swing timing), `isAnimatedDoor`. The three.js layer (`createDoorInstance`/`buildDoors`) turns that into a rotating hinge pivot + panel mesh, reusing `world.ts`'s `loadMeshTemplate`/`applyMeshFit`/`normalizeModelToFootprint` (now exported) rather than reimplementing GLB loading — same "instant stand-in box, async GLB swap, keep the box on failure" pattern as furniture.
- **Tick choice**: no new timer. `main.ts`'s existing render loop calls `doorInstance.update(sdt, simPos, simPath)` once per frame, right after `agent.update(sdt)`, using the SAME sim-time `sdt` the animation mixer advances on — pausing the game freezes doors mid-swing, 2×/3× speeds them up, exactly like the character's animation. `simPath` comes from a new `SimAgent.getPathPoints()` (`game/sim.ts`): current position + remaining unvisited waypoints, empty while not moving — the "current nav path" the crossing test needs.
- **Hinge convention**: `AssetDef.door.hingeOffset` is defined in the door's CANONICAL model-local frame — local **+X is the door's long/swing axis**, local **+Z is its thickness axis** — the same frame regardless of which way the door is oriented in the map. Placing a door rotates that frame by a fixed "base yaw" so its long axis lies along the wall it sits in: `'horizontal'` orientation (wall runs along world X) → base yaw 0°; `'vertical'` orientation (wall runs along world Z) → base yaw 90°. This exactly reproduces the dimensions of the OLD plain-marker box world.ts already drew per orientation (`BoxGeometry(1.0,h,0.14)` horizontal / `BoxGeometry(0.14,h,1.0)` vertical) — same shape, now parented under a hinge pivot instead of centered on the doorway. The panel's local offset from the hinge is `-hingeOffset` (same canonical frame), so the closed panel's center exactly reproduces the door's original `at` position — verified analytically in `test/doors.test.ts` for both orientations, not just by construction.
- **Path-crossing test**: ports the Unreal `BP_Door` "transform consecutive path-point pairs into door-local space, open only if a pair crosses the doorway plane" description directly: the doorway is a fixed-coordinate plane (X for vertical doors, Z for horizontal) spanning `door.width ?? 1.0` meters along the other axis; a path segment crosses if its endpoints are on opposite sides of the plane AND the interpolated crossing point falls within that width. `pathCrossesDoorway` is true if ANY consecutive pair in the sim's current path crosses.
- **Open/close state machine** (`doorShouldBeOpen(withinTrigger, pathCrosses, currentlyOpen)`) — the exact §7.1 rules, no hidden state beyond the caller's own `currentlyOpen` flag: not within trigger → closed; within trigger AND crossing → open; within trigger, not crossing, but ALREADY open → stays open (never close mid-transit, even if the path changed underneath the sim); within trigger, not crossing, not already open → stays closed (standing near a door the sim isn't walking through never opens it). All 7 branches PIE-equivalent-tested in `test/doors.test.ts`.
- **Swing timing**: linear interpolation (not exponential smoothing like the Unreal `FInterpTo` — the brief's "rotate open... over openSeconds" reads as a fixed duration, which linear satisfies exactly and deterministically) at a rate of `openAngleDeg / openSeconds` (or `/closeSeconds` closing), clamped so it never overshoots the target and reaches it exactly at the end of the configured duration; a `0`-second config snaps instantly (guards div-by-zero).
- **`world.ts` integration**: the pre-existing plain-marker door loop now skips any door whose `assetId` resolves to an asset with a `door` block (`doorDef?.door` truthy) — those are rendered/animated by `doors.ts`'s `buildDoors()` instead (its group is added as a child of `world` in `main.ts`, so hot-reload disposal/rebuild is automatic via the existing `disposeGroup(world)` path). Doors without an `assetId`, or pointing at a non-door-capable asset, get the EXACT old bare-opening-marker behavior — zero visual/behavioral change, old maps stay valid as speced. No circular import: `world.ts` inlines the one-line door-capability check itself rather than importing `doors.ts` (which imports FROM `world.ts` for the mesh-loading helpers) — `doors.ts`'s own `isAnimatedDoor` (identical logic, `Map`-based) exists as a tested, reusable pure export for anything else that needs the same check.
- **Nav**: untouched by design — door panels are visual/animated only, never registered with `nav.ts`; doorways were already carved walkable independent of any door mesh (§2b/CLAUDE.md-equivalent nav carving predates this slice), so nothing needed to change to keep them passable.
- **Shipped data**: `data/tuning.json` gained `doors: { openSeconds: 0.5, closeSeconds: 0.5, openAngleDeg: 90, triggerDistance: 1.5 }` (triggerDistance in meters — map `gridSize` is 1, i.e. 1 grid unit = 1 meter — sized so the door swings open a beat before the sim, at `walkSpeed` 2 m/s, reaches it). `data/assets.json` gained category `"door"` and asset `door_basic` (`buyable: false`, `footprint: [1, 0.12]`, `door: { hingeOffset: [-0.5, 0] }` — hinge at the left edge of a 1m-wide panel, everything else sparse/tuning-derived). **No real door GLB exists** (`public/models/` only has `bed.glb`/`character.glb`/`tv.glb`) — `door_basic.mesh` points at `models/door.glb`, which fails to load exactly like every other not-yet-imported furniture mesh in this repo, and `doors.ts` keeps its box stand-in (same philosophy as every other placeholder asset, not door-specific). `data/maps/condo.json`'s all 4 door entries got `assetId: "door_basic"` so the feature is visible in the shipped map — animated stand-in panels swing open/closed as the sim paths through each doorway.
- **Tools**: `tools/assets.html` gained a "Door" card (hinge offset x/z, open angle, open/close seconds, trigger distance — all sparse except hingeOffset, which the door block needs to mean anything) shown only when `a.category === 'door'`; the category `<select>` already offered every id in `assets.json`'s `categories` array, so adding `"door"` to that array (data-only) was the entire "make door offerable" change — no dropdown code changed. `tools/map.html`'s Doors-mode inspector gained a "door asset" `<select>` (options: "(none)" + every door-category asset by name) that writes/deletes `doors[].assetId`.
- **Tests**: new `test/doors.test.ts` (`npx tsx`, 45 assertions) covers `resolveDoorConfig`'s sparse-merge + null cases, the hinge/panel transform's closed-position round trip for both orientations, `segmentCrossesDoorway`/`pathCrossesDoorway` (in-range/out-of-range/same-side/degenerate-on-plane/custom-width cases), `distanceToDoor`, all 7 branches of `doorShouldBeOpen`, `stepDoorAngle` (half/full duration, overshoot clamping, asymmetric open/close rates, zero-second snap, sub-stepped convergence), and `isAnimatedDoor`. `tools/asset-editor.test.mjs` extended with a door-category-only-section-visibility + sparse round-trip case (hingeOffset + openAngleDeg set, openSeconds/closeSeconds/triggerDistance left absent, asserted absent in the PUT payload). `test/map-editor.test.mjs` extended with an assetId round-trip (dropdown defaults to "(none)", offers exactly the door-category assets, set/clear round-trips through `doors[].assetId`). All pre-existing suites re-run clean, including `test/nav.test.ts` against the real (now `assetId`-bearing) `condo.json` — confirms doors staying non-nav-blocking. `npx tsc --noEmit --strict --target es2020 --moduleResolution bundler --module esnext --skipLibCheck game/*.ts` clean. `npm run dev` sanity check: zero console errors (only the expected "could not load mesh"-style warnings, now including `door_basic`'s missing GLB); Map Editor's door-asset dropdown and Asset Editor's Door card verified live in a real browser (not just jsdom) against the actual shipped `condo.json`/`assets.json`.

### 7.2 Asset facing direction (the "normal" vector)

- `AssetDef` gains `facingDeg: number` (default 0): the direction the object faces in **model-local space**, expressed as a yaw in degrees (0 = the model's local −Z after `meshFit.yawOffsetDeg`, i.e. what the game already treats as "toward the camera at rot 0" — the implementing agent must verify and document the exact convention against `world.ts`'s placement math). An instance's **world facing** = instance `rot` + `facingDeg`. A single yaw is exactly the "normal vector telling me where the object is facing" the designer asked for, in the friendliest editable form (degrees, not a vector triple) — tools may *display* it as an arrow.
- **Consumers (game)**: a shared `useSpotFor(instance)` helper — the interaction stand/approach point becomes "footprint edge along world facing + small clearance" instead of any current pivot/center heuristic (fridge opens from its front, WC approached from the front…); seat-aware logic ("sit in front of the TV") screens candidate seats by the half-space test `dot(seatPos − tvPos, tvFacing) > 0` and the seat's own facing toward the target — this replicates the Unreal Phase-A seat-in-front-of-screen logic that needed a hardcoded RightVector exception for the TV mesh; with `facingDeg` per asset that exception class disappears.
- **Tools**: Asset Editor gets the `facingDeg` field (and `meshFit`, and `buyable` from §7.1); Map Editor draws a small facing arrow on every placed object (read-only indicator, instance rot + asset facingDeg) so the designer can see at a glance which way furniture faces.

### 7.2 as-built (2026-07-14)

- **The exact convention (verified against `world.ts`, not guessed)**: `world.ts` places every object with `obj.rotation.y = degToRad(placed.rotDeg)` and touches nothing else on that outer rotation; `sim.ts`'s `SimAgent` faces its direction of travel with `rotation.y = atan2(dx, dz)`. Three.js's Y-rotation of a local +Z point `(0,0,1)` by angle θ lands at world `(sinθ, 0, cosθ)` — exactly `atan2`'s inverse — so "`rotation.y = 0` ⇒ local **+Z** is forward" is already the ONE convention the whole codebase uses (the sim's own facing and every placed object), not two. The draft text above guessing "local −Z" was superseded: **`facingDeg = 0` means the model's local +Z (after any `meshFit.yawOffsetDeg` correction) is the object's front**, and `worldFacingDeg = instance.rotDeg + (facingDeg ?? 0)`. No real furniture GLBs beyond `tv.glb`/`bed.glb`/`character.glb` exist yet (rest are procedural stand-ins per `PROGRESS.md`/roadmap), and even those two didn't give a conclusive visual "front" cue in the Asset Editor's low-poly preview at this resolution — so the convention is analytically derived and code-verified rather than eyeballed; verify visually once real distinct-looking meshes land.
- **Helpers — new `game/facing.ts`** (pure math, no THREE dependency, headless-tested in `test/facing.test.ts`): `worldFacingDeg(instance, def)`, `facingVector(worldDeg)` (unit XZ direction), `useSpotFor(instance, def, tuning)` (footprint-edge-along-facing stand point + `tuning.interaction.useSpotClearance` clearance, using the oriented-box support-function formula `|dir.x|*halfW + |dir.z|*halfD` — ports the Unreal prototype's `BP_Asset.DoAction` stand-spot math from CLAUDE.md), `isInFrontHalfSpace(point, instance, def)`, and `viewingPointFor(instance, def, tuning)` (target position projected forward by `tuning.interaction.seatViewDistance` — ports the Unreal prototype's `RightVector·400` viewing-point). `instance` is `{ pos: [x,z], rotDeg }`; THREE call sites (`sim.ts`, `autonomy.ts`) build it from an `Object3D` via a small `facingInstanceOf()` adapter (`rotDeg = radToDeg(obj.rotation.y)`, valid because world.ts never touches that outer rotation after placement).
- **New tuning group `tuning.interaction`** (optional, mirrors the `character?` precedent so old tuning fixtures/tests stay valid): `{ useSpotClearance: 0.4, seatViewDistance: 2.5 }`. Code falls back with `?? 0.4` / `?? 2.5` when absent, same pattern as `sim.ts`'s existing `sitHeight ?? 0.25`.
- **Approach-point consumer — `sim.ts`'s `SimAgent.orderAction`**: gained an optional 4th param `targetDef?: AssetDef`. When supplied (both call sites — `main.ts`'s tap handler and `autonomy.ts`'s `maybeAct` — already had the target's `AssetDef` in hand and now pass it), the walk-to point for the plain-target branch is `useSpotFor(...)` instead of the old "nearest walkable cell to the raw pivot" heuristic; if the front cell is unreachable (asset flush against a wall) it falls back to the old pivot heuristic automatically. The seat branch (seat-aware actions) is **unchanged** — the sim still walks to and sits ON the seat's own pivot, not in front of it — and either way the final sit/lie snap onto the seat/target (`applyPose`) is untouched, so this only changes *where the sim walks to*, never the final pose. Omitting `targetDef` (e.g. `test/locomotion.smoke.ts`, which predates asset data) preserves the exact old behavior, so no existing test needed to change.
- **Seat-aware screening — `sim.ts`'s `findSeatFor`**: rewritten to (1) filter candidate seats to the target's front half-space (`isInFrontHalfSpace`), then (2) rank survivors by nearest distance to `viewingPointFor(target)` — replacing the old "nearest `seatTarget` object regardless of side" heuristic (which, combined with a shipped-data bug below, meant seat-aware selection was completely non-functional before this slice) and the Unreal prototype's TV-specific `RightVector` mesh-normal exception (CLAUDE.md ANIMATION_PLAN Phase A) — any seat-aware asset now gets the same treatment for free via its own `facingDeg`.
- **Shipped-data bug found + fixed (`data/assets.json`)**: `seatTarget: true` was on the **TV** (nonsensical — nothing can sit on a TV) and absent from `sofa`/`armchair`/`dining_chair` (the actual seats). Since `findSeatFor` excludes the action's own target object, this meant `watch_tv`'s `seatAware` had **zero** eligible candidates in the shipped map — the feature was silently dead on arrival, unrelated to facing but only surfaced while wiring the half-space screen. Fixed: `seatTarget: true` moved onto `sofa`/`armchair`/`dining_chair`; removed from `tv`.
- **Shipped `facingDeg` values chosen: none — zero overrides needed.** Walked every placed instance in `data/maps/condo.json` against the convention above and confirmed the designer's existing `rotDeg` choices already produce sensible facings with `facingDeg` absent (= 0) everywhere:
  - TV (`rotDeg 180`) and sofa (`rotDeg 0`) face each other exactly (verified both analytically and by `test/facing.test.ts`'s half-space + viewing-point-ranking checks against these literal numbers — sofa ranks distance 0 from the TV's viewing point vs. ~3.16 for the off-axis armchair, which still passes the half-space filter but loses the ranking).
  - Every appliance placed against an outer wall (fridge, stove, sink ×2, toilet, shower, both bookshelf instances) faces *away* from its adjacent wall and into the room under `facingDeg=0`, so `useSpotFor` lands the approach point in open floor space, not inside a wall.
  - The one non-cardinal instance (armchair, `rotDeg 315`) still resolves to a sensible diagonal "into the room" facing and correctly stays a *candidate* seat for `watch_tv` (passes the half-space test) while losing the ranking to the sofa — a useful incidental validation that the algorithm degrades gracefully on off-grid rotations.
  - Conclusion documented rather than encoded: if a future asset's real mesh (once real GLBs replace the stand-ins) turns out authored front-on a different local axis, set that asset's `facingDeg` (and/or `meshFit.yawOffsetDeg` if the mesh itself needs visually correcting) then — nothing here is load-bearing on non-existent geometry today.
- **`meshFit` application point — `game/world.ts`'s `attachMesh`**: applied in a new `applyMeshFit(model, fit)` call immediately after `normalizeModelToFootprint` (i.e. corrections are *deviations from* the automatic footprint fit, not a replacement for it). `scale` multiplies on top of the auto-fit scale (uniform number or per-axis `[x,y,z]`); `yawOffsetDeg` rotates the loaded model in place (mirrors the character rig's existing `yawOffsetDeg` wrapping trick in `loadRiggedCharacter`) — `facingDeg` is defined in terms of the model's orientation *after* this correction, so the two fields compose without coupling; `yOffset` nudges vertically post-grounding. All three are independently sparse.
- **`buyable`**: landed as speced (§7.1) — `AssetDef.buyable?: boolean`, absent = true, no consumer yet besides the Asset Editor checkbox (Buy/Sell mode is the future consumer, still blocked per §4 item 10).
- **Asset Editor (`tools/assets.html`)**: `facingDeg` number input (blank = 0) with hint text under the Placement & seating card; `buyable` checkbox in the Asset card (checked by default when absent, unchecking writes `buyable:false` explicitly — the sparse direction is inverted from `seatTarget`'s, since `buyable`'s default is `true` not `false`); new "Mesh fit" card between Mesh and Actions with a uniform-scale number field, a per-axis x/y/z triple (array form — whichever the designer touches most recently wins; per-axis fields don't support "unsetting" a single axis back to absence, only the uniform field fully self-prunes when cleared), `yawOffsetDeg`, and `yOffset` — all sparse (`meshFit` itself is deleted once every sub-field is cleared). `tools/asset-editor.test.mjs` extended: buyable default/uncheck round-trip, facingDeg sparse round-trip, meshFit uniform + per-axis scale, yawOffsetDeg, sparse pruning of the whole `meshFit` object, and confirms an untouched new asset ships none of the three keys.
- **Map Editor arrow (`tools/map.html`)**: the canvas already drew a facing tick for `+Z rotated by rotDeg`; extended to `+Z rotated by (rotDeg + asset.facingDeg)` and upgraded from a bare line to a small filled triangle arrowhead at the tip (still read-only, still minimal — no interaction/selection logic touches it). Not covered by `test/map-editor.test.mjs` since that suite's own canvas drawing is skipped under jsdom (no 2D context — documented in the suite's header comment); the full existing suite re-runs clean regardless.
- **Verification**: `npx tsc --noEmit --strict --target es2020 --moduleResolution bundler --module esnext --skipLibCheck game/*.ts` clean. New `test/facing.test.ts` (`npx tsx`) covers `worldFacingDeg`/`facingVector`/`useSpotFor`/`isInFrontHalfSpace`/`viewingPointFor` in isolation, including the exact shipped sofa/TV numbers. Full suite re-run clean: all `test/*.mjs` + `tools/*.mjs` (`node`), all `test/*.ts` (`npx tsx`, one at a time), `test/meshfit.test.mjs` (`npx tsx` — pre-existing node-vs-tsx quirk, unrelated to this slice, still there). `npm run dev` sanity check: game loads with zero console errors (only the pre-existing "could not load mesh, keeping stand-in" warnings for the asset ids that don't have real GLBs yet), all `/data/*.json` fetches 200.

### 7.3 Accidents

- **Accident types are assets too**: `category: "accident"`, `buyable: false` (fire, water puddle, debris…). They get mesh/footprint/interactions through the existing Asset Editor for free. Their `interactions` are the cleanup actions (mop, extinguish — authored in the Interaction Editor like any action); their `environmentScore` should typically be negative while present. New field on accident-category assets: `clearedBy: string[]` — action ids whose completion on the accident instance despawns it.
- **Per-asset risk config** on any normal asset: `accidents: [{ accidentId, trigger: "onUse", baseChancePercent, placement: "on" | "adjacent", adjacentRange: [1, 2], modifiers: [{ var, pctAt0, pctAtMax }] }]`.
  - **Roll timing**: once, when a sim *finishes* an action on the asset (`trigger: "onUse"` is the only trigger for now; the enum exists so future triggers — time-based, idle — slot in).
  - **Risk formula**: `chance = clamp(baseChancePercent + Σ modifier contributions, 0, 100)`; each modifier reads a stat via the **quest condition namespace** (`needs.<id>`, `skills.<id>` — reuse `game/quests.ts`'s path resolution, don't reinvent) and contributes a linear interpolation from `pctAt0` (stat at 0) to `pctAtMax` (stat at its max). Example: kitchen fire `baseChancePercent: 2`, modifier `{ var: "skills.cooking", pctAt0: +15, pctAtMax: −2 }` — a novice cook risks ~17%, a master ~0%.
  - **Placement**: `"on"` spawns the accident on the base asset's own cells (kitchen fire ON the stove); `"adjacent"` picks a random free, in-bounds, non-wall cell 1–2 grid squares away (`adjacentRange`) — the designer's "leak" case. If no free cell exists, fall back to "on".
- **Runtime**: accident *instances* are runtime state (spawned into the world + nav/tap systems like placed objects), NOT written into the map file; shape must be serializable for the future save system (same convention as `QuestRunner.serialize()`). 
- **Interaction hierarchy (the designer's exact rule)**: if an accident instance's footprint **overlaps** a base asset's footprint, the accident's interactions REPLACE the base asset's in the tap menu (impossible to cook while the kitchen is on fire) and autonomy must likewise skip the blocked asset's actions. If they do **not** overlap, there is no hierarchy — the accident is just its own independent tappable object alongside the asset.
- **Tools**: the Asset Editor gains (a) nothing special for accident-category assets beyond the `clearedBy` action multi-select, and (b) an "Accidents" risk section on normal assets — accident dropdown (accident-category assets only), trigger, base %, placement + range, modifier rows with var dropdowns fed from `stats.json` (no free-typed ids, as always).

### 7.3 as-built (2026-07-14)

- **New module `game/accidents.ts`**, split like `doors.ts`: pure logic (risk math, footprint geometry, placement selection, the instance registry incl. `serialize()`/`restore()`) is headless-tested in `test/accidents.test.ts` with zero THREE dependency; a thin `AccidentsController` three.js layer turns that into live meshes and wires into `main.ts`'s tap/autonomy/environment code paths, reusing `world.ts`'s exported `loadMeshTemplate`/`applyMeshFit`/`normalizeModelToFootprint` for the same "instant stand-in box, async GLB swap, keep the box on failure" mesh philosophy as furniture and doors.
- **Roll point in the action lifecycle**: hooked to `SimAgent.onActionStop` (`game/main.ts`), NOT to the auto-stop branch in `simTick`'s gain loop. This is deliberate: several actions (e.g. "cook") ship `primaryNeed: null` and therefore never auto-stop on their own (main.ts's gain loop only auto-stops when a `primaryNeed` is set), so gating the roll on natural completion only would make their risk unreachable. `onActionStop` fires for every stop reason — natural auto-stop, player cancel, a fresh order overriding the current one — matching §7.3's "once, when a sim finishes an action" without over-specifying what "finishes" excludes. The stopped action's target asset is looked up by `userData.assetId`; if it's a normal asset with an `accidents` array, `AccidentsController.rollFor` runs; if it's itself an accident instance (`userData.accidentKey` set), `maybeCleanup` runs instead. The risk-modifier `EvalContext` is built from the SAME snapshot shape `quests.tick()` already uses each decay tick (needs/skills via `Object.fromEntries`, funds/vars/quests from the live `QuestRunner`, time from the game clock) and evaluated through `game/quests.ts`'s now-exported `resolveVar` — no separate path-resolution logic exists.
- **Risk formula**: `computeAccidentChance` = `clamp(baseChancePercent + Σ modifier contributions, 0, 100)`; each modifier lerps a %-point contribution from `pctAt0` (stat at 0) to `pctAtMax` (stat at max) via `accidentModifierContribution`. `needs.<id>` always interpolates against a fixed max of 100 (matching `stats.ts`'s hardcoded need clamp); `skills.<id>` uses that skill's own `max` from `stats.json` (the shipped `cooking` skill's max is 10, not 100 — verified against the exact §7.3 example: novice cook 2+15=17%, master cook 2−2=0%). Any other/unknown var path (unresolvable id, or a non-numeric namespace like `vars.*`/`quests.*.state`) contributes exactly 0 via `resolveVar`'s existing "unknown → undefined, never throw" contract — documented in code, not a silent gap.
- **Placement algorithm**: `"on"` always spawns at the base asset's own position (same footprint cells as the base — the point of a stove fire). `"adjacent"` (`findAdjacentCell`) scans a Chebyshev ring of grid cells (`adjacentRange`, default `[1,2]`) around the base asset's cell, keeping cells that are `nav.ts`-walkable (already excludes walls and static furniture footprints) AND pass an extra dynamic-occupancy check against every other currently-live accident instance's footprint (nav.ts's static bake has no notion of runtime accidents), then picks one via an injectable `rng` (defaults to `Math.random`, fixed in tests). Falls back to `"on"` placement when no candidate qualifies, per spec.
- **Hierarchy implementation**: purely geometric, not tied to "who triggered it" — `AccidentRegistry.findBlocking(basePos, baseRotDeg, baseFootprint)` checks AABB overlap (`footprintRect`/`rectsOverlap`, same 90°-step width/depth swap rule as `nav.ts`'s `bakeNavGrid` and `facing.ts`'s `placedHalfExtents`) against every live instance and returns the first match. `main.ts`'s tap handler calls `accidents.blockingFor(hit.object, asset)` right after resolving the tapped asset; if blocked, `resolveTapAssetId` (a small pure decision function, independently unit-tested) redirects both the menu's asset/actions AND the walk/order target onto the accident's own live `THREE.Object3D` (`accidents.groupFor(key)`) — so `agent.orderAction`'s target, and therefore the eventual `onActionStop`'s cleanup lookup, is the accident instance, not the base asset. `Autonomy.maybeAct`'s candidate loop takes an optional `AccidentsController` (5th constructor param, defaulted `undefined` so old call sites/tests are unaffected) and skips any candidate object where `accidents.isBlocked(obj, def)` — the ENTIRE base asset is excluded, not just individual actions. The SEPARATE no-duplicate-stacking rule (`AccidentRegistry.canSpawn`) is deliberately keyed by triggering instance (`THREE.Object3D.uuid` as `baseKey`, no schema change to `world.ts`'s placed-object `userData` needed) rather than geometry — a puddle that drifts under an unrelated chair still blocks that chair (geometric hierarchy), but that's independent from "don't let the SAME stove roll two simultaneous fires."
- **Environment wiring**: DONE, not just reported — `main.ts`'s `environmentScore()` already iterated `data.map.placedObjects` (static map data); it now ALSO sums `accidents.registry.all`'s live instances' `environmentScore` via the same asset-id lookup map, so a live fire/puddle drags the computed Environment need down for as long as it exists and the contribution disappears the instant it's cleaned up (verified by the shipped `-10`/`-5` scores on `fire`/`water_puddle`).
- **Persistence / hot-reload semantics**: `AccidentRegistry` (the pure half) has `serialize()`/`restore()` in the exact `QuestRunner` shape/convention (§3.3) — `{ instances: AccidentInstanceRecord[], seq }`, ready for a direct `JSON.stringify`/parse once a save system exists (none does yet, same caveat as quests/needs/skills — runtime-only, resets on reload). Separately, `AccidentsController.reattach(world)` solves the DATA-hot-reload case (tuning/assets/map edited on disk while playing): `main.ts`'s `watchData` callback rebuilds `world` from scratch every poll (`buildWorld(data)` has no notion of runtime accidents, since they're never in map data) — `reattach` is called right after `scene.add(world)` to re-parent every still-live instance's `THREE.Group` into the fresh world group, so editing data mid-game never wipes an active fire/puddle. `AccidentsController.restore(s)` (full save-state restore) additionally rebuilds the live groups from scratch, for the future save-load path.
- **Shipped example data**: `data/interactions.json` gained `extinguish` (no need/skill effect) and `mop` (`hygiene +0.5`), both `autonomyEligible: false`, `primaryNeed: null`, `animation: "stand_use"`. `data/assets.json` gained category `"accident"` and two accident-category assets — `fire` (`environmentScore: -10`, `interactions: ["extinguish"]`, `clearedBy: ["extinguish"]`) and `water_puddle` (`environmentScore: -5`, `interactions: ["mop"]`, `clearedBy: ["mop"]`), both `buyable: false`, no real mesh yet (box/flat-box stand-ins, same "keep the box on failure" philosophy as every other not-yet-imported mesh in this repo). Risk config: `stove` gained `accidents: [{ accidentId: "fire", trigger: "onUse", baseChancePercent: 2, placement: "on", modifiers: [{ var: "skills.cooking", pctAt0: 15, pctAtMax: -2 }] }]` — the §7.3 spec's own worked example, verbatim; `shower` gained `accidents: [{ accidentId: "water_puddle", trigger: "onUse", baseChancePercent: 4, placement: "adjacent", adjacentRange: [1,2] }]` (the "leak" case, no modifiers).
- **Tools**: `tools/assets.html`'s `load()` now also fetches `stats.json` (needed for the modifier var dropdown). Accident-category assets get a "Cleanup" card: a `clearedBy` multi-select checklist scoped to the asset's OWN already-checked `interactions` (not the full action list — clearedBy can only reference actions this asset actually offers). Every other asset gets an "Accidents" card: one collapsible risk box per `accidents[]` entry (accident dropdown fed from accident-category assets only, a fixed single-option "onUse" trigger select, base % number, placement select that live-re-renders to reveal/hide the adjacent-range pair, and per-modifier rows with a needs/skills-namespaced var dropdown + pctAt0/pctAtMax numbers), plus "+ Add accident risk"/"+ Add modifier"/remove buttons. All arrays (`accidents`, `modifiers`, `clearedBy`) are sparse — pruned back to an absent key when emptied, never left as a stray `[]`. `tools/asset-editor.test.mjs` extended with the full round-trip (risk add/edit incl. the placement-switch re-render and modifier sub-row, exact PUT payload) plus the referential-integrity check that the accident dropdown lists ONLY accident-category assets.
- **Tests**: new `test/accidents.test.ts` (`npx tsx`, 51 assertions) covers the risk formula (base-only, modifiers at 0/max/mid, clamping both directions, unknown skill id, non-numeric namespace, `needs.<id>`'s fixed-100 max, multi-modifier summing), `rollAccident`'s injectable rng, footprint geometry (rotation swap at ~90°/~270°, overlap incl. an exact edge-touching non-overlap case), `findAdjacentCell`/`planAccidentPlacement` (in-range selection, seeded-rng determinism, range exclusion, no-free-cell → null, out-of-bounds rejection, on/adjacent/fallback-to-on, default range), and the full `AccidentRegistry` (spawn/despawn, no-duplicate-stacking incl. the "different accident type on the same base is allowed" and "null baseKey never collides" cases, geometric `findBlocking` independent of triggering instance, the `resolveTapAssetId`/`isAutonomyBlocked` decision wrappers, `shouldDespawnOnCleanup`, and a serialize/restore round-trip incl. seq-counter continuity and a deep-copy-not-shared-refs check). Full existing suite re-run clean: all `test/*.mjs`+`tools/*.mjs` (`node`), all `test/*.ts` (`npx tsx`, one at a time) incl. `test/meshfit.test.mjs`'s pre-existing node-vs-tsx quirk (unrelated, unchanged). Strict type gate clean. `npm run dev` sanity check: zero console errors (only the expected "could not load mesh" warnings, unchanged set — `fire`/`water_puddle` don't appear there since they're never in `map.placedObjects`, only spawned at runtime); Asset Editor's new Cleanup/Accidents cards verified live in a real browser against the actual shipped `assets.json` (stove's fire risk and fire's clearedBy both render correctly).

### 7.4 Tool navigation

- A shared nav bar on every tool page + the game: new `tools/nav.js` (plain non-module script so jsdom tests are unaffected) that injects a compact tab strip — Game, Assets, Interactions, Tuning, Map, Animations, Quests — highlighting the current page. Each `tools/*.html` (and `index.html`) includes it with one `<script src>` line. New tools must add themselves to the one list in `nav.js`.

### 7.4 as-built (2026-07-14)

- **File**: `tools/nav.js`, a self-guarded IIFE. Injects its own `<style>` + DOM, keyed off `location.pathname` (matches `/tools/<file>.html` against its own `TOOLS` list; `/` or `/index.html` → the game). Idempotent (checks for `#condo-toolnav`/`#condo-toolnav-corner` before injecting) and wrapped in try/catch so it can never break a host page. **Include pattern**: one line, `<script src="/tools/nav.js"></script>`, placed just before `</body>` in all six `tools/*.html` files and `index.html` — the absolute `/tools/nav.js` path resolves correctly from every page since Vite (via `server.js`'s fallthrough to `vite.middlewares`) serves `tools/*.js` as a normal static asset with no server changes needed (verified: `GET /tools/nav.js` → 200, `Content-Type: text/javascript`).
- **index.html decision**: the game's HUD (`game/ui.ts`) already fills the top edge — `#needs-panel`/`#skills-panel` at `top:8px` left/right, `#time-bar` top-center, `#quest-toasts` at `top:56px` center — so a top strip would collide. Per §7.4's own escape hatch, `index.html` instead gets a small fixed gear button (`#condo-toolnav-corner`, `right:8px; bottom:40px`) that expands a link panel upward; that corner sits clear of `#devbar` (full-width bottom strip), `#quest-panel` (bottom-left), and `#action-menu`/`#activity-chip` (bottom-center). All six `tools/*.html` pages get the plain top-strip variant instead (inserted as the first child of `<body>`, non-sticky, so it never fights each page's own `header { position: sticky; top: 0 }` — the strip just scrolls away first).
- **Layout compensation**: `assets.html` and `interactions.html` hardcode `#layout { height: calc(100vh - 53px) }` against their own header's height; `nav.js` adds one scoped override rule, `#layout { height: calc(100vh - 53px - 32px) !important; }`, so the extra 32px strip doesn't cause a few pixels of unwanted page scroll. This rule is unconditionally injected but is a no-op on every page without a `#layout` element.
- **Tests**: new `test/toolnav.test.mjs` (jsdom) — loads `tools/nav.js`'s source as an inline `<script>` (jsdom doesn't fetch external `<script src>` without `resources: "usable"`, which is also exactly why adding the include line to every other tool's HTML doesn't affect *their* existing suites at all) and asserts: top strip renders + is the first body child + correct active tab per page, no corner button on tool pages; the game page gets the corner button instead with correct active tab, toggle-open/close-on-outside-click behavior; and double-injection is a no-op. Future tools: add one entry to the `TOOLS` array in `tools/nav.js` and one `<script src="/tools/nav.js"></script>` line before `</body>`.

### 7.6 Buy/Sell mode — locked design (2026-07-14, Sims Buy Mode references received)

The designer supplied Sims 1/3/4 Buy Mode screenshots. Distilled to this game's scope (player buys/sells/moves furniture inside designer-made homes; NO build mode — walls/floors/doors stay Map Editor territory):

- **Asset icons (designer requirement: "each asset has an icon")**: new `AssetDef.icon?: string` — an image path under `public/` (same drop-in workflow as meshes/sprites). Catalog fallback when absent: a category-colored tile with the asset's initials — never a broken image. Asset Editor gains the `icon` field with a thumbnail preview next to it.
- **Mode toggle**: a Buy button on the game HUD opens buy mode; **sim time freezes** while in it (mixer/needs/quests/doors/GIFs all already run on sim time — matches Sims pausing in Buy Mode, and avoids nav-rebake-vs-moving-sim conflicts). Grid overlay shows over the floor; normal needs/skills panels hide; funds display prominent (Sims-style).
- **Catalog** (bottom bar, mobile-first, per the Sims 4 reference): category tabs derived from `assets.json` categories (only categories with at least one purchasable item), item cards (icon + name + buy price), search box. An asset is purchasable iff `buyable !== false` AND (no `requiresQuestUnlock` flag OR `QuestRunner.isAssetUnlocked(id)` — the §3.3 tie-in; `requiresQuestUnlock?: boolean` is added to `AssetDef` now, exposed in the Asset Editor).
- **Placement flow**: tap a catalog item → ghost of the asset follows taps/drags on the grid (½-cell snap, like the Map Editor), rotate button (90° steps), live validity tint (green = in bounds + no footprint overlap with placed objects/walls; red = invalid), ✓ confirm / ✗ cancel buttons (touch-sized). Confirm: deduct funds (insufficient funds blocks with feedback before the ghost even starts — gray out unaffordable cards), spawn into the world, rebake nav.
- **Select / move / sell placed furniture**: in buy mode, tapping a placed object selects it (highlight + Sims-4-style action chips): Move (drag with the same ghost/validity rules), Rotate, Sell (refunds `sellPrice`, removes, rebakes nav). Accident instances and door-category assets are not selectable/sellable in buy mode.
- **Persistence**: player purchases/moves/sells are **runtime state** (same convention as quests/accidents — `serialize()`/`restore()` shape ready for the future save system, resets on reload). They must NOT be written back into the designer's `data/maps/*.json`. Buy-mode changes layer over the map's `placedObjects` (an overlay list of added instances + moved/sold designer-object overrides).
- **Funds**: lives in `QuestRunner` (single money source of truth — quest rewards and buy/sell flow through the same balance). HUD shows funds inside buy mode always, and outside via the existing quest/HUD surface (small persistent funds chip).

### 7.6 as-built (2026-07-14)

- **New module `game/buymode.ts`**, split like doors.ts/accidents.ts: pure logic (catalog filtering, placement validity, the runtime overlay registry incl. `serialize()`/`restore()`, buy/sell/move flows, icon-fallback color/initials) is headless-tested in `test/buymode.test.ts` (65 assertions) with zero THREE dependency; a thin three.js layer (`BuyModeController`) turns that into ghost placement, live meshes for purchased furniture, and the designer-object override patching wired into `main.ts`'s tap/HUD/nav-rebake code paths — same "instant stand-in, async GLB swap" mesh philosophy as furniture/doors/accidents (reuses `world.ts`'s now-exported `makeStandIn`/`attachMesh`).
- **Overlay model (never writes to `data/maps/*.json`)**: `BuyOverlay` is a runtime-only registry, layered over `map.placedObjects` — `additions: OverlayAddition[]` (player purchases, generated `buy#N` keys, don't exist in map data at all) + `overrides: Map<designerIndex, {type:'moved'|'sold', ...}>` (keyed by a designer object's INDEX into `map.placedObjects`, stable for the life of one loaded map). `world.ts`'s `buildWorld()` now tags each placed object's `userData.placedIndex` with that same index specifically so `BuyModeController` can find "the live Object3D for designer object #i" after every rebuild and patch it (hide if sold, reposition if moved) — mirrors `AccidentsController.reattach`'s re-parenting pattern for the player-addition groups, which have no map-data counterpart at all. `effectiveInstances()`/`effectivePlacedObjects()` produce the merged (designer-minus-sold-plus-moved, plus additions) list in `MapData.placedObjects`'s own shape — both nav rebake and placement-validity overlap checks consume this ONE list.
- **Nav rebake**: a single `rebakeNav()` closure in `main.ts` (`grid = bakeNavGrid({ ...data.map, placedObjects: buyMode.effectivePlacedObjectsList() }, data.assets); agent.retune(...)`) replaced the old direct `bakeNavGrid(data.map, data.assets)` call in BOTH the ordinary hot-reload handler and every buy/sell/move action — so overlay changes always feed the nav grid, including across a tuning/map/asset edit landing mid-shopping-session. Placement validity (`isValidPlacement`) reuses `accidents.ts`'s exported `footprintRect`/`rectsOverlap` (same 90°-step width/depth swap convention as nav.ts/facing.ts) against that same effective-instance list, plus a new `wallRect()` helper (walls are axis-locked by the Map Editor, so an axis-aligned bounding rect thickened by `world.ts`'s `WALL_T` 0.12m is exactly correct — not an oriented-rect test).
- **Sim-time freeze — a render-loop `sdt` override, NOT `hud.speed` mutation**: `main.ts`'s render loop now computes `const sdt = buyMode.active ? 0 : dt * hud.speed;` instead of always `dt * hud.speed`. Deliberately chosen over saving/restoring `hud.speed`: the player's chosen 1×/2×/3×/pause selection is completely undisturbed by entering/exiting buy mode — it simply doesn't advance while shopping and resumes exactly where it left off, with no save/restore dance and no misleading "paused" button state while a different speed is queued underneath. Real-time UI (click cue, camera pan/zoom) still runs on raw `dt`, matching the locked design. Freezing `agent.update()` this way also means the sim can never be mid-move when a buy-mode nav rebake happens; the one remaining edge case (the sim WAS mid-route the instant buy mode opened, and a rebake during shopping invalidates that stale path) is handled by a safety net in `onBuyClose`: if any buy/sell/move actually landed this session and the agent is still (frozen) mid-path, cancel it in place via `agent.goTo(sim.x, sim.z)` rather than risk resuming through now-solid furniture.
- **Tap routing**: `main.ts`'s single `TapInput` callback branches on `buyMode.active` first (`if (buyMode.active) { handleBuyModeTap(hit); return; }`) before the ordinary tap-to-go/action-menu logic — no changes to `game/input.ts` needed. `handleBuyModeTap`: while placing/moving (a ghost is up), any tap uses `hit.ground` (always populated by `TapInput.raycast`'s independent ground-plane test, even when an object was also hit) to reposition the ghost via `BuyModeController.moveGhostTo` — snapped to half-cells, revalidated live. Otherwise, tapping a placed object resolves it via `BuyModeController.instanceForObject` (walks up `userData.assetId`/`placedIndex`/`buyKey`, matching `input.ts`'s own tap-resolution convention) and selects it (Move/Rotate/Sell chips) if `isSelectableForSell`; any other tap deselects. **Deliberate simplification, documented in code**: placement/move follow taps, not a live drag-follow — consistent with this game's existing tap-only interaction model (tap-to-go, tap-to-act); a true drag-follow ghost is a possible future enhancement, not required by any test here.
- **HUD (`game/ui.ts`)**: new `#funds-chip` (persistent, bottom-right, always shows current funds) + `#buy-button` (bottom-right, above it) — positioned per §8's layout budget, clear of the full-width `#devbar`, `tools/nav.js`'s corner gear (`right:8px; bottom:40px`), and the bottom-center action-menu/activity-chip. `#buy-bar` (bottom sheet: funds readout + search + Exit, category tabs, a horizontally-scrolling card row — icon `<img>` with an `onerror` swap to a category-colored initials `<div>` "fallback tile", never a broken image) opens/closes via `setBuyModeActive(active)`, which also hides the needs/skills panels and any open action-menu/activity-chip per the locked spec. `#buy-ghost-controls` (Rotate/Confirm/Cancel) and `#buy-selection-chips` (name + Move/Rotate/Sell-with-price/Close) are separate bottom-center pill bars, shown/hidden by `BuyModeController`'s state transitions. All new CSS matches the existing dark-panel/pill-button visual language (`rgba(20,26,40,.82/.92)` panels, `rgba(90,120,190,...)` accent, same border-radius/font conventions) — narrow-viewport tweaks live under the pre-existing `@media (max-width: 500px)` block (§8) rather than a new breakpoint.
- **Catalog rule, exactly as speced**: `isPurchasable(def, isUnlocked)` = `def.category !== 'accident'` AND `def.buyable !== false` AND (`!def.requiresQuestUnlock` OR `isUnlocked(def.id)`). Door exclusion is NOT a category special-case — `door_basic` already ships `buyable:false` (§7.1), so it falls out of the `buyable` check alone, per spec ("no special-casing"); the accident-category exclusion IS an explicit special-case (belt-and-braces per spec, since a future accident asset could theoretically omit `buyable:false`). `isUnlocked` is `quests.isAssetUnlocked(id)`, wired straight to the existing §3.3 `QuestRunner` method — no changes needed there.
- **Schema**: `AssetDef.icon?: string` (image path under `public/`, same drop-in convention as `mesh`/sprites) and `AssetDef.requiresQuestUnlock?: boolean` (sparse, absent = not gated) — both landed in `game/data.ts`. `tools/assets.html` gained a "Catalog icon" card (path field + live `<img>` thumbnail with an error handler explaining the initials-tile fallback) and a `requiresQuestUnlock` checkbox in the Asset card (next to `buyable`, same sparse-write pattern). `tools/asset-editor.test.mjs` extended: icon/requiresQuestUnlock round-trip (including the new asset shipping neither key).
- **Selling designer-placed objects**: implemented exactly as speced — refunds `sellPrice`, marks the designer index `sold` (never deleted from `map.placedObjects` itself, since that array is never mutated). Re-buying the same asset afterward creates a brand-new `additions` entry, not a restoration of the original — verified explicitly in `test/buymode.test.ts`'s "re-buy after sell" scenario. Accident instances are automatically unselectable (they never appear in `effectiveInstances` — they live only in `AccidentRegistry`'s own separate runtime list, never in `map.placedObjects`); door-category assets are excluded via `isSelectableForSell`'s explicit category check (belt-and-braces, since no door is normally placed via `placedObjects` at all — doors live in `map.doors[]`).
- **Tunables**: none added — every numeric knob here (ghost tint alpha, grid overlay color) is pure cosmetic, per the task's own "don't invent tunables for pure cosmetics" guidance; the one genuinely gameplay-relevant number (footprint/wall-overlap validity) reuses existing geometry (`footprint`, `WALL_THICKNESS` matching `world.ts`'s hardcoded `WALL_T`) rather than a new tuning field.
- **Verified live** (`npm run dev` + browser, the user's already-running dev server — not a fresh one): Buy button opens the bar, needs/skills panels hide, funds chip reads `§20,000` (from `tuning.economy.startingFunds`) in both the persistent chip and the bar's own readout, category tabs list exactly the categories with a purchasable item (Seating/Beds/Appliances/Plumbing/Electronics/Surfaces/Decor — `door`/`accident` correctly absent), cards render name+price, category switching and search both filter correctly (and combine, e.g. searching "arm" while on the Beds tab correctly returns nothing since Armchair is category `seating`). Picking a catalog item opens the ghost controls; confirming at an intentionally-colliding spot (the ghost's default spawn, map center, happens to collide with the shipped map's dining furniture cluster at (7,5)) was correctly REJECTED (`console.warn('buy mode confirm failed: invalid_placement')`, funds unchanged) — proving the placement-validity gate is wired end-to-end from HUD click through to the overlay; Cancel correctly discarded the ghost with no funds change. Exit correctly restored the needs/skills panels and hid the buy chrome. Zero console errors throughout. **Known sandbox limitation** (same as §8's screenshot note): this session's Browser pane reported the WebGL canvas at 0×0 client size and `computer` screenshots consistently timed out, so the actual 3D ghost rendering (tint color, grid overlay) and real pointer-drag placement/selection of a specific placed object couldn't be visually screenshot-verified here — those paths are covered instead by `test/buymode.test.ts`'s pure-logic assertions (identical convention to how `doors.ts`/`accidents.ts`'s three.js layers are verified: unit-test the pure half, sanity-check the wiring, no dedicated three.js-layer test). **Not yet human-tested**: real touch/mouse drag-to-select a specific placed object and the Move/Rotate/Sell chip flow against actual rendered geometry (logic verified by unit tests; wiring verified by the console-clean HUD interaction session above).
- **Tests**: new `test/buymode.test.ts` (`npx tsx`, 65 assertions) covers catalog filtering (buyable/accident-exclusion/door-via-buyable/quest-gate/afford/category/search, `catalogCategories` category-tab derivation), placement math (`snapToHalfCell`/`snapPos`/`normalizeRotDeg`/`rotateStep`, `wallRect` for both wall axes, `isValidPlacement` bounds/wall/object-overlap incl. `excludeKey` self-overlap-while-moving), icon fallback (`iconFallbackColor`/`iconFallbackInitials`), the full `BuyOverlay` (add/move/sell designer + player instances, `effectiveInstances`/`effectivePlacedObjects`, `isSelectableForSell`, serialize/restore round-trip incl. a deep-copy-not-shared-refs check), `attemptBuy`/`attemptSell`/`attemptMove` (insufficient funds, invalid placement, accident/buyable-false rejection, designer vs. player instance handling, unknown-key rejection), and the dedicated re-buy-after-sell scenario. `tools/asset-editor.test.mjs` extended (icon + requiresQuestUnlock sparse round-trip, thumbnail element presence, new-asset ships neither key). Full existing suite re-run clean: all `test/*.mjs`+`tools/*.mjs` (`node`), all `test/*.ts` (`npx tsx`, one at a time) incl. `test/meshfit.test.mjs`'s pre-existing node-vs-tsx quirk (unrelated, unchanged) and `test/locomotion.smoke.ts`. Strict type gate (`npx tsc --noEmit --strict --target es2020 --moduleResolution bundler --module esnext --skipLibCheck game/*.ts`) clean.

### 7.7 Character overhead marker — locked design (2026-07-14, Sims plumbob reference)

The designer wants a Sims-style icon floating above the sim's head, swappable through the tools like every other visual, accepting **either a 2D sprite (png/jpg/webp/gif) or a 3D mesh (glb)**.

- **Config lives in `tuning.character.marker`** (it's character rig config, not an asset): `{ mesh?: string, yOffset?: number, scale?: number, spinDegPerSec?: number, bobAmplitude?: number, bobHz?: number }`. `mesh` goes through the SAME §7.5 extension detection: image → billboard sprite (GIFs animate on sim time); `.glb` → mesh. **Empty/absent `mesh` = built-in default: a flat-shaded green octahedron** (THREE.OctahedronGeometry — the plumbob silhouette, no file needed, matches the reference screenshot out of the box).
- **Behavior**: anchored above the head (`heightMeters + yOffset`), follows the character; 3D markers spin at `spinDegPerSec` (sprites don't spin — they billboard); optional gentle vertical bob (`bobAmplitude`/`bobHz`). All motion on **sim time** (pause freezes spin/bob/GIF, consistent with everything else). Defaults in `data/tuning.json`, no magic numbers.
- **Tool surface**: the **Animation Mapper** (`tools/animations.html`) — it's the dedicated character-rig page and already edits/saves the whole `tuning.character` block. New "Overhead marker" section: mesh path field (GLB or image, same hint language as the Asset Editor's mesh field), yOffset/scale/spin/bob numbers, live preview on the character in its existing 3D viewport.
- Future (not this slice): tie marker color/icon to mood or active quest — the mesh-path-in-tuning design means a quest reward or mood system can later just swap the path.

### 7.7 as-built (2026-07-14)

- **New module `game/marker.ts`**, split like doors.ts/accidents.ts/sprites.ts: pure logic (sparse config resolution, bob/spin math, mesh-kind classification, the synthetic `AssetDef` fed to `attachMesh`) is headless-tested in `test/marker.test.ts` with zero THREE dependency; the thin three.js layer (`createMarkerInstance`) turns that into a live group tracked every frame from `main.ts`'s render loop.
- **Anchor/tilt finding (verified by reading `sim.ts`, not guessed)**: `SimAgent.applyPose` only ever rotates the character ROOT (`object.rotation.x = -Math.PI/2`) for the pre-rig CAPSULE stand-in (`!agent.hasRig`) — the real rigged character never rotates its root for sit/lie, only `position.y` changes (seat/bed perch height); the seated/lying visual itself comes from the animation clip. So a literal `sim`-child marker would be safe from tilt in the common case — but `main.ts`'s `loadCharacter()` calls `sim.clear()` on every rig (re)load (first load, or a hot-reload `meshPath` edit), which would silently delete a marker parented there. **Decision**: the marker is an INDEPENDENT top-level object (a sibling of `sim` in the scene, not a child), and its `pivot.position` is copied from `sim.position` every frame — translation only, `sim`'s rotation is deliberately never read. This makes the marker immune by construction to any current or future root-rotation trick (the capsule tip hack, a facing snap, a future pose) rather than merely because today's specific cases were audited, and it sidesteps the `sim.clear()` deletion hazard entirely. Consequence: 3D spin needs no counter-rotation (the pivot never inherited a rotation to cancel) and sprites billboard for free (`THREE.Sprite` ignores rotation regardless).
- **Known limitation, documented not fixed**: the marker's vertical anchor is `sim.position.y + heightMeters + yOffset`, correct while standing/walking (`sim.position.y` is 0). While sitting/lying, `sim.position.y` shifts to `sitHeight`/`lieHeight` (seat-surface alignment, not the seated character's true head height), so `heightMeters` no longer represents the seated/lying sim's real head height and the marker reads as floating too high above a seated/lying sim. Precise pose-aware anchoring would need bone/socket lookups — out of scope for this slice, same "documented, future refinement" precedent as this repo's other approximate transforms (e.g. §8's HUD-collision accepted tradeoff).
- **Default mesh**: `THREE.OctahedronGeometry` (radius = half of a synthetic 1m "footprint" every marker mesh kind is fit to, for consistent `scale` behavior), Y-elongated ×1.4 for a plumbob silhouette, flat green (`0x3ec93e`) `MeshBasicMaterial` — deliberately basic/unlit (not `MeshStandardMaterial`) so the marker reads the same bright green at night as in daylight, matching a Sims plumbob's always-visible role rather than dimming with `applyDayNight`'s lighting.
- **Mesh resolution reuses §7.5's pipeline exactly, no reimplementation**: a synthetic `AssetDef` (`markerAssetDef`, id `overhead-marker`, `footprint:[1,1]`, `sprite:{orientation:'billboard'}`) is handed to `world.ts`'s exported `attachMesh`, which already does image-vs-GLB extension detection (`classifyMeshPath`) and the "instant stand-in, async swap, keep the stand-in on any failure" dance — the default octahedron IS that stand-in, so a bad/missing mesh path just leaves it in place, exactly like every other mesh call site.
- **Scale convention (deliberate deviation from ordinary assets)**: `scale` is NOT baked into the loaded mesh via `meshFit` (which only reapplies on a reload) — it's applied continuously to the outer wrapper group every frame instead, so `scale`/`yOffset`/`spinDegPerSec`/`bobAmplitude`/`bobHz` all stay instantly live-tunable for every mesh kind (including the async GLB/sprite ones) with zero rebuild. Only the `mesh` PATH triggers an async rebuild (`meshSig` check inside `update()`) — exactly the spec's "recreate marker on marker.mesh change, re-tune numbers in place otherwise" rule, verified live (see below).
- **Spin**: applies to both 3D kinds (the default octahedron AND a loaded `.glb`) — normalized world-yaw degrees via `spinAngleDeg`, set directly on `visual.rotation.y` each frame (no counter-rotation needed, see the anchor finding above); explicitly zeroed for the `'image'` kind (`THREE.Sprite` always faces the camera regardless of rotation — spinning it would be a no-op at best).
- **Sim-time wiring**: `main.ts`'s render loop calls `marker.update(sdt, data.tuning.character)` right after `anim?.update(sdt)`, using the same speed/pause-scaled `sdt` the mixer/doors/sprites already use — pause freezes spin/bob/GIF frames, 2×/3× speeds them up. GIF frame advance reuses the exact §7.5 mechanism (`visual.traverse((o) => o.userData.spriteUpdate?.(dtSeconds))`), since the marker isn't under `world` (so main.ts's existing `world.traverse` sweep doesn't reach it) and needs its own one-line sweep.
- **Hot-reload**: no character block → no marker (mirrors the rig itself, which no-ops without `meshPath`); `main.ts`'s `watchData` callback creates the marker if a character block newly appears and disposes it if the block disappears. No other retune plumbing needed — `update()` re-resolves `tuning.character.marker` fresh every call, so numeric edits apply next frame automatically.
- **Shipped data**: `data/tuning.json`'s `character.marker` ships WITH explicit default values (not an empty object) — `{ mesh:"", yOffset:0.35, scale:0.3, spinDegPerSec:90, bobAmplitude:0.05, bobHz:0.8 }` — tuned against the shipped 1.55m character height (documented `_source`, same convention as every other tunable in the file).
- **Tool (`tools/animations.html`)**: new "Overhead marker" section in the Character column — mesh path text field (blank = built-in plumbob, same hint language as the Asset Editor's mesh field) + 5 sparse number fields (blank = use the game-side default, placeholder shows the exact default value), following the Asset Editor's `meshFit`/`sprite`-card ensure/prune convention (`setMarkerField`: blank clears the key, whole `marker` object pruned once empty). Live 3D preview: `createMarkerInstance` imported directly from `/game/marker.ts` (never reimplemented) and created once against a fixed origin anchor (the preview character never moves, so there's no `sim.clear()` hazard to worry about here); updated every preview frame with `AnimTool.character()` (real time, not sim time — this tool has no pause/speed concept).
- **Tests**: new `test/marker.test.ts` (`npx tsx`, pure logic only — `createMarkerInstance` is browser-only, same precedent as `sprites.ts`'s `createSpriteInstance`) covers `resolveMarkerConfig`'s sparse merge (incl. `zero is a legit override, not absent`), `classifyMarkerMesh`'s delegation to `sprites.ts`'s `classifyMeshPath`, `bobOffset`'s sine wave at quarter/half/full-period marks, `spinAngleDeg`'s `[0,360)` normalization incl. negative rates, `markerAnchorHeight`, and `markerAssetDef`'s shape. `test/animation-editor.test.mjs` extended: marker section renders with correct blank-field/placeholder-default state, mesh path leading-slash normalization, sparse per-field round-trip (set/clear individually, whole-object pruning on last-key-clear), non-numeric rejection, and the exact sparse `PUT` payload. Full existing suite re-run clean (all `test/*.mjs`+`tools/*.mjs` via `node`, all `test/*.ts` via `npx tsx` one at a time incl. `test/meshfit.test.mjs`'s pre-existing node-vs-tsx quirk, unrelated/unchanged). Strict type gate clean.
- **Live-verified in a real browser** (`npm run dev`, the user's already-running dev server, via a temporary `window.__markerDebug()` hook added/removed for this session only — same "temporarily instrument, verify, revert" convention as §7.5's sprite fallback-chain check): default octahedron renders above the sim's head (`pivotPos` tracked `simPos` exactly across walking), `visualPos.y` oscillated in the expected bob range (anchor 1.9 ± 0.05), `visualScale` matched the configured `0.3`, `visualRotY` advanced over time (spin confirmed live) — **caught and fixed a real bug this way**: spin was initially gated on `classifyMarkerMesh(...) === 'model'` only, silently excluding the default octahedron (a 3D mesh that should spin) from spinning at all; fixed to gate on `!== 'image'` instead. Also live-verified, via direct `PUT /api/data/tuning.json` edits against the running server: `scale` hot-reload applies in place with no rebuild (`0.3→0.6` reflected within one poll, no mesh reload); switching `mesh` to a real GLB (`bed.glb`) rebuilds the visual to a loaded `Group`; switching `mesh` to an existing test image (`public/models/test_fire_sprite.png`, kept from §7.5) rebuilds it to a `Sprite` with rotation correctly pinned at 0. Zero console errors throughout every check. Test data was reverted to the shipped defaults after verification. Animation Mapper tool: screenshot-verified the green plumbob floating above the T-posed preview character with the Overhead marker section's fields rendering correctly.

### 7.5 Sprite / GIF visuals for assets (designer request 2026-07-14, aimed at accidents)

- **Same drop-in-folder workflow as GLBs**: the designer puts an image file (`.png`/`.jpg`/`.webp`/`.gif`) under `public/` and points the asset's existing `mesh` path at it — the loader detects by extension whether to load a GLB or an image. No separate field, no upload pipeline (yet).
- **Rendering**: images become a textured plane sized from the asset's `footprint` (respecting `meshFit.scale`/`yOffset`), with a new optional `AssetDef.sprite?: { orientation?: "billboard" | "flat", fps?: number }` — `billboard` (default) always faces the camera (fire, smoke); `flat` lies on the floor (puddle, debris, scorch marks). Alpha transparency respected.
- **Animated GIFs**: decoded to frames and advanced on **sim time** (pause freezes them, like the mixer and doors). Prefer the browser `ImageDecoder` (WebCodecs) API with graceful degradation to a static first frame where unsupported; `fps` overrides the GIF's own frame delays if set.
- General to ALL assets (an accident is just an asset), but accidents are the motivating case — fire/puddle stand-in boxes should be replaceable by a single GIF drop.
- **Preview-gap fix (2026-07-14, found by the designer)**: the original slice extended the Asset Editor's form but not its 3D preview, which GLTF-loaded everything — an image mesh showed "Could not load /models/fire.gif" in the preview even though the game rendered it fine. The preview module now imports `classifyMeshPath`/`createSpriteInstance` from `/game/sprites.ts` (the game's OWN pipeline, per the never-reimplement rule) and renders image meshes as the real sprite — billboard/flat per the asset's config, GIFs animating on real time in the preview — with a staleness token (`previewSeq`) so a slow decode never clobbers a newer selection. The mesh field label/placeholder now say "GLB or image path".

### 7.5 as-built (2026-07-14)

- **New module `game/sprites.ts`**, split like doors.ts/accidents.ts: pure logic (extension classification, sparse `sprite` config resolution, GIF frame-timing math, footprint-based plane sizing) is headless-tested in `test/sprites.test.ts` with zero THREE/browser dependency; a thin three.js layer (`createSpriteInstance`) turns an image path into a live visual.
- **Extension detection lives in ONE place**: `classifyMeshPath(path)` (`.glb`/`.gltf`/unrecognized/extensionless → `'model'`; `.png`/`.jpg`/`.jpeg`/`.webp`/`.gif` case-insensitively, query-string-tolerant → `'image'`) is called from a single shared function, `game/world.ts`'s now-**exported** `attachMesh(group, def, opts?)` — previously a private GLB-only helper. This is the ONE mesh-loading call site every consumer now goes through: `world.ts`'s `buildWorld()` (furniture, unchanged call), `doors.ts`'s `createDoorInstance` (door panels), and `accidents.ts`'s `buildGroup` (accident instances) — the latter two previously had their own near-duplicate copies of the GLB-load-then-swap logic, which this slice deleted in favor of the one shared function. Furniture and accidents get sprite support "for free" as a result; nothing else needed to change in either module besides swapping their inline loader for a call to `attachMesh`.
- **Door panels explicitly REJECT sprites** (the spec's "your call, document it"): `attachMesh(panel, def, { allowSprite: false })` from `doors.ts`. Rationale: a billboard sprite always faces the camera regardless of the hinge pivot's rotation (so it would never visually swing), and a floor-flat plane doesn't read as a vertical door panel at all — neither sprite orientation can represent a swinging door, so a door asset pointed at an image keeps its GLB-only stand-in box and logs a console warning rather than silently rendering something wrong.
- **Billboard mechanism (documented choice)**: `orientation: "billboard"` (the default) uses a real `THREE.Sprite` — three.js's built-in always-faces-the-camera primitive (full billboarding, not a manual Y-axis-only spin) — with `sprite.center.set(0.5, 0)` so the anchor sits at the sprite's bottom-center, matching how a GLB is grounded at y=0 by `normalizeModelToFootprint`. `orientation: "flat"` is an ordinary `PlaneGeometry` rotated flat (`rotateX(-Math.PI/2)`) with a `FLAT_EPSILON` (0.005m) lift off the floor to avoid z-fighting — a real mesh, so it reads correctly from any camera angle (including top-down), unlike a sprite. Both use `MeshBasicMaterial`/`SpriteMaterial` with `transparent: true, alphaTest: 0.5` (flat additionally `depthWrite: false, side: THREE.DoubleSide`) for clean alpha edges with no z-sorting artifacts.
- **Sizing**: `spritePlaneSize(def)` = `footprint` scaled by `meshFit.scale` (uniform number or per-axis `[x,y,z]`, z ignored — no depth on a 2D plane) — the same "meshFit is a deviation from the automatic fit" convention as GLBs (§7.1/§7.2). `meshFit.yOffset` nudges the plane/sprite vertically post-grounding, same field, same meaning, as a GLB.
- **GIF decode path**: prefers the browser `ImageDecoder` (WebCodecs) API — `decodeGifFrames()` fetches the file, decodes every frame up front into small canvases plus that frame's delay (`VideoFrame.duration` is in **microseconds**, converted to ms; a codec reporting no duration falls back to 100ms/frame), then during playback draws the current frame into a single shared `THREE.CanvasTexture`'s backing canvas and flips `needsUpdate` only when the computed frame index actually changes (not every render tick) — "keep the per-frame cost cheap" per the spec. Where `ImageDecoder` doesn't exist, or decoding throws, falls back to a plain `THREE.TextureLoader` load (the same path every `.png`/`.jpg`/`.webp` always takes) — for an animated GIF this naturally renders as a static first frame, since the browser's own `<img>`-style decoding used internally by `TextureLoader` doesn't animate.
- **Sim-time wiring**: `SpriteInstance.update(dtSeconds)` accumulates `dtSeconds*1000` into an internal ms counter and looks up the frame via `frameIndexAtTime` — called from `game/main.ts`'s render loop with `sdt` (already pause/1×/2×/3×-scaled, the exact same value the animation mixer and doors receive), via **one line**: `world.traverse((o) => { o.userData.spriteUpdate?.(sdt); })`. `attachMesh` stores the update callback on `group.userData.spriteUpdate` once the sprite is ready — since `world.traverse` recursively visits every descendant (furniture groups, `doors.group` and accident groups are both parented under `world`), this single traversal advances GIF frames anywhere in the scene with zero additional per-caller wiring. No new signature changes were needed on `buildWorld`, `buildDoors`, or `AccidentsController` to make this work.
- **Fallback chain verified live** (not just by construction): temporarily pointed the placed `phone` asset's `mesh` at a 404'ing `.gif` path — console showed the GIF-decode attempt failing (`ImageDecoder` IS available in the dev browser used for this check), the code falling back to `loadStatic()`, THAT failing too (also 404), and `attachMesh`'s outer `.catch()` logging "keeping stand-in" — the phone's ordinary category-colored stand-in box stayed visible throughout, exactly like a missing GLB. Reverted the data change immediately after.
- **Billboard and flat orientations both verified live**: temporarily pointed `phone`'s mesh at a hand-made test PNG (`public/models/test_fire_sprite.png`, a 64×64 translucent orange circle, generated once via the `canvas` devDependency — kept in the repo as a tiny, genuinely useful designer reference image for "what a sprite asset's mesh file should look like," not wired into any shipped asset) — billboard orientation rendered a camera-facing translucent circle at the phone's position (screenshot-verified), switching to `sprite:{orientation:"flat"}` rendered the same circle lying flat on the floor instead (screenshot-verified). Reverted the data change immediately after; the PNG file itself was kept per the task's own "your call" allowance.
- **Tools**: `tools/assets.html` gained a "Sprite" card (orientation select defaulting to billboard, fps number — both sparse, same `ensureSprite`/`pruneSprite` pattern as `meshFit`/`door`) shown only when `isImageMesh(a.mesh)` — a small inline extension-list check duplicated from `classifyMeshPath`'s rule (the plain inline script deliberately doesn't import `game/*.ts`, same convention as every other tool, e.g. the Quest Editor's own `slugify`). `tools/asset-editor.test.mjs` extended: no sprite card while an asset's mesh is a `.glb`, card appears once the mesh field is edited to an image path, orientation/fps sparse round-trip through the exact PUT payload, and an untouched accident asset (still `.glb`) ships no `sprite` key.
- **Shipped data**: unchanged — per §7.5, nothing in `data/assets.json` points at an image by default; `fire`/`water_puddle` keep their `.glb` stand-in paths, ready for a designer to drop a GIF in and edit the path whenever real art lands.
- **Tests**: new `test/sprites.test.ts` (`npx tsx`, pure logic only — `createSpriteInstance`/`decodeGifFrames` are browser-only and NOT headless-testable, per the module's own doc comment) covers `extensionOf`/`classifyMeshPath`/`isGifPath` (case-insensitivity, query strings, unknown/extensionless → safe `'model'` fallback), `resolveSpriteConfig`'s billboard default, `frameDurationsMs`'s fps-override-vs-native-delays (incl. `fps<=0` being treated as "no override"), `frameIndexAtTime` (uniform and non-uniform per-frame durations, multi-loop wraparound, empty/single-frame/all-zero-duration edge cases, defensive negative-elapsed handling), and `spritePlaneSize`'s uniform/per-axis `meshFit.scale`. Full existing suite re-run clean: all `test/*.mjs`+`tools/*.mjs` (`node`), all `test/*.ts` (`npx tsx`, one at a time) incl. `test/meshfit.test.mjs`'s pre-existing node-vs-tsx quirk (unrelated, unchanged). Strict type gate (`npx tsc --noEmit --strict --target es2020 --moduleResolution bundler --module esnext --skipLibCheck game/*.ts`) clean. `npm run dev` sanity check: zero console errors on load (only the expected "could not load mesh" warnings for not-yet-imported GLBs, unchanged set).

---

## 8. Mobile polish & PWA (as built, 2026-07-14)

Roadmap Phase 5 / §4 item 11. Two halves: an installable PWA shell and a small set of layout/touch fixes for narrow phones. Both scoped to the **game page only** — the tool pages stay desktop-oriented per the task brief ("low effort, not the focus"); they already had a working `viewport` meta tag and needed no changes beyond the pre-existing `tools/nav.js` strip.

### PWA — manifest, icons, service worker

- **`public/manifest.webmanifest`**: name/short_name "Condo Life", `display: "standalone"`, **`orientation: "portrait-primary"`** — chosen because the whole HUD (`game/ui.ts`: needs/skills panels top-left/right, time-bar top-center, quest panel bottom-left, action menu bottom-center) and the camera (`game/camera.ts`) are laid out for a tall aspect ratio; nothing about the current design reads better landscape. `background_color`/`theme_color` both `#1b2230` (matches `index.html`'s existing `<body>`/`#boot` background and `<meta name="theme-color">`, so there's no color flash between the OS splash screen and the page paint).
- **Icons**: `public/icons/icon-192.png` / `icon-512.png`, flat `#1b2230` square with a rounded `#5a9fd6` accent panel (the game's own toast/hover-box accent color) and a "CL" glyph — generated programmatically by **`scripts/generate-icons.mjs`** (`node scripts/generate-icons.mjs`) using the `canvas` devDependency already in the repo (same package `test/sprites.test.ts`'s hand-made test PNG used, §7.5). **The designer should replace both PNGs with real art whenever it's ready**; the script stays in the repo as a documented, repeatable "regenerate the placeholders" command, not a one-off. Only two sizes, `purpose: "any"` — no maskable variant yet (task scope: "a flat colored square with a glyph/text is fine").
- **Service worker — `public/sw.js`** (ES module, imports its routing predicate from `public/sw-routing.js`), registered from **`index.html` only** (never the tool pages) via a small inline script guarded by `'serviceWorker' in navigator`, on `window.load`, with `{ type: 'module' }`. **Caching strategy, and why it's safe for hot-reload**:
  - `/data/*.json` and `/api/*` → **network-only**, the fetch handler never touches the cache for these at all (`isDataOrApiPath()` in `sw-routing.js`, headless-tested in `test/sw-routing.test.mjs`). This is the one rule that must never regress: the game polls `/data/*.json` every 2s (`game/data.ts`'s `watchData()`) and the tool constellation GETs/PUTs through `/api/data/*` — serving anything but a live network response there would silently break tuning hot-reload and every tool's save. The SW also only ever intercepts `GET` requests (`if (req.method !== 'GET') return;`), so tool `PUT`/`DELETE` calls are never even looked at by the fetch handler, belt-and-suspenders on top of the path check.
  - Everything else (app shell, tool pages, built JS/CSS, models, manifest, icons) → **network-first**, falling back to a versioned cache (`condo-life-v1`) only when the network request fails (offline); successful responses are opportunistically cached for that fallback. Chosen over cache-first specifically because `npm run dev` serves `game/*.ts` through Vite's own dev transform (HMR, query-stamped URLs) — a cache-first policy would risk serving a stale dev module. Network-first means the cache is simply never consulted while the network is up, in dev or prod, so it can't interfere with HMR or a tool's live-edit workflow, while still giving real offline support once installed.
  - **Verified live** (`npm run dev` + browser, SW active): `navigator.serviceWorker.getRegistrations()` showed the SW `activated`; a direct `fetch('/data/tuning.json', {cache:'no-store'})` still returned the live file (200, `cache-control: no-store` from `server.js`); the game's own 2s hot-reload poll continued returning fresh 200s for all `/data/*.json` files with the SW active (checked via the browser's network log); a tool page (`/tools/assets.html`) loaded with zero console errors and no SW registered on it (registration script isn't present there).
- **Build check**: `npm run build` (vite build) succeeds; `dist/` contains `manifest.webmanifest`, `sw.js`, `sw-routing.js`, and `icons/icon-192.png`/`icon-512.png` at the root, alongside the existing `assets/`/`models/`/`index.html` (Vite's `publicDir: 'public'` convention copies all of these verbatim, no build config changes were needed). Note: the tool pages (`tools/*.html`) and `data/*.json` are **not** part of the Vite build output today (pre-existing — the tools/dev-server workflow was never wired into `vite build`'s multi-page config); out of scope for this slice, unrelated to the PWA files themselves landing correctly.
- **Busting the SW cache after a future deploy**: bump `CACHE_VERSION` in `public/sw.js` — the `activate` handler already deletes every cache key that doesn't match the current version, so a version bump is a one-line change with no other cleanup needed.

### Mobile polish

- **Touch/selection hardening (`index.html`)**: `<body>` gained `-webkit-user-select: none; user-select: none; -webkit-touch-callout: none; -webkit-tap-highlight-color: transparent;` — the HUD (`game/ui.ts`) is appended into this same `<body>`, so one rule covers the canvas and every HUD panel/button; `canvas` and `#app` explicitly carry `touch-action: none` too (reinforcing the existing `body { touch-action: none }`, which already blocked double-tap-zoom/pinch — the browser computes touch-action as the intersection of an element and its ancestors, but making it explicit on the elements that matter costs nothing and documents intent). `#app` also got `overscroll-behavior: none` alongside the pre-existing `html, body { overscroll-behavior: none }`, closing the pull-to-refresh gap on the canvas specifically.
- **Safe-area insets (`game/ui.ts`)**: every edge-pinned HUD element now pads for `env(safe-area-inset-*, 0px)` instead of a bare pixel offset: `#needs-panel`/`#skills-panel`/`#time-bar` (`top`+ `left`/`right`), `#quest-panel`/`#action-menu`/`#activity-chip` (`bottom`), `#quest-toasts` (`top`). Falls back to `0px` on non-notched devices, so this is a pure addition with no effect on the existing desktop/simulator layout.
- **devicePixelRatio cap**: already in place before this slice (`game/main.ts`: `renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2))`) — verified, documented here, not touched.
- **Resize/orientation handling**: `game/main.ts`'s existing `window.resize` listener (renderer resize + `cam.resize()`) is joined by a new `orientationchange` listener that re-runs the same handler after a 200 ms delay — a safety net for mobile browsers that briefly report stale `innerWidth`/`innerHeight` during the rotation animation before `resize` fires; harmless no-op double-call when dimensions already settled.
- **HUD collision at narrow widths — the main audit finding**: at ~375-414px portrait, the top-center `#time-bar` (~210px wide, unshrunk) geometrically cannot share the top row with both the top-left `#needs-panel` and top-right `#skills-panel` (168px each) — combined width alone (~546px) exceeds the viewport. Fix (`game/ui.ts`), all under one `@media (max-width: 500px)` block **matching the existing JS breakpoint exactly** (see pitfall below): both side panels shrink to 136px and start **collapsed** by default (extending the pre-existing "skills panel starts collapsed under 500px" JS rule in the `Hud` constructor to also collapse the needs panel — previously only skills did), and `#time-bar` drops from `top: 8px` to `top: 46px` (clearing the now header-only collapsed panels), with `#quest-toasts` dropping in tandem to `top: 90px`. **Verified live** (browser at 375×812 and 490×900, via computed `getBoundingClientRect()` — the sandboxed WebGL canvas wouldn't screenshot reliably in this session, so verification is CSS/JS-computed layout rather than a pixel screenshot, per the task's own fallback allowance): at 375px, collapsed needs/skills panels span `y:[8,40]`, time-bar spans `y:[46,80]` — zero overlap. At 700px (desktop-like), panels stay expanded at their original 168px width and the unshrunk time-bar sits centered between them with clear gaps on both sides — unaffected. **Accepted tradeoff, documented in a code comment**: a player who deliberately taps a panel header open on a narrow screen can make it visually extend under the time-bar (verified: expanded needs-panel at 375px reaches `y:152`, overlapping the time-bar's `y:[46,80]` in a small corner) — this is a user-initiated expansion, not the default-load collision the audit targeted, and fixing it would need either a measured-height JS layout pass or moving the time-bar entirely off the top row; left as a candidate future refinement, not blocking this slice.
- **Pitfall found and fixed during verification**: the CSS media query was initially written as `max-width: 480px`, while the existing JS collapse trigger was `window.innerWidth < 500`. That 20px mismatch (widths 481–499px) meant panels would collapse via JS but the compensating `#time-bar` top-offset wouldn't apply, reproducing the exact collision the fix was meant to remove. Caught by explicitly testing 490px width mid-implementation; fixed by changing the media query to `max-width: 500px` to match the JS threshold. Flagged here because the two live in different files/languages (JS constructor logic vs. a CSS media query) with no shared constant — a future change to either breakpoint must update both.
- **Tool pages**: `viewport` meta tag was already present on all six `tools/*.html` files (verified, no change needed); confirmed loading with zero console errors at a 375px viewport. One `tools/tuning.html` page does have horizontal overflow at 375px (`scrollWidth` 579 vs `clientWidth` 375) from its own desktop-oriented grid layout — pre-existing, not introduced by this slice, and explicitly out of scope per the task brief ("low effort, not the focus").
- **New test**: `test/sw-routing.test.mjs` (`node`, 12 assertions) — the only new pure-logic surface from this slice (`isDataOrApiPath()`), following the "pure logic module, headless-tested" convention used throughout (`doors.ts`, `accidents.ts`, `sprites.ts`). The HUD/SW-registration/manifest changes themselves are DOM/CSS/network-plumbing with no new pure logic to unit-test beyond that, per the task's own "otherwise document why no new suite" allowance.
- **Verification**: full existing suite re-run clean (all `test/*.mjs`+`tools/*.mjs` via `node`, all `test/*.ts` via `npx tsx` one at a time, incl. `test/meshfit.test.mjs`'s pre-existing node-vs-tsx quirk, unrelated/unchanged) plus the new `test/sw-routing.test.mjs`. Strict type gate (`npx tsc --noEmit --strict --target es2020 --moduleResolution bundler --module esnext --skipLibCheck game/*.ts`) clean. `npm run build` succeeds with the PWA files landing correctly in `dist/` (see above). `npm run dev` + browser sanity check at 375×812 and other widths: zero console errors, SW active and not interfering with hot-reload or a tool page load, HUD collision resolved with no regression at desktop widths.
