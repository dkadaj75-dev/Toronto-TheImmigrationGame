When the action name is too long, the button adapts correctly but overlaps with others. Either find a rule to change the angle compared to the center or the radius.
When in social tab tool, I should be able to add as many assets as possible to perform an action, add a button to add/remove assets or categories
Since TVs or Lamps have a ON/OFF system, doors have a OPEN/close system with different models and states and that we already started implementing the "state" system, I would like to generalize that: Every asset can have different states (designer can add/remove as they wish) and depending on the state, different interactions are available (similar to having something on/off) which leads to different access to needs, an example: if we have a murphy bed then if the bed is closed, we can only open it, changing its mesh and collisions, but once opened, the bed offers the opportunity to sleep. The character should be able to "read through the states", meaning open the bed, then sleep if it wants to sleep.
Still for assets: I am thinking of having asset categories for the designer (i.e. not necessarily the same than in-game) that we can check/uncheck to give access to features and avoid a page full of features, for example who needs to see the light settings when it is a basic chair? So the designer can choose one or several categories: electronics (features are light, sound, on/off etc.), comfort (beds, chairs, sofa, with the sit/ lie features etc.), garbage cans, plumbing (also we need to add a risk of leaks for this one, that will spawn a puddle), etc. But all assets will still have the basic settings as well (the model, interactions, the states etc.), this is more to have a clean set up.
Still for the assets: I want to be able to set the exact use location through a box with the facing vector, OR the character (we already kinda implemented it) to visualize the action being performed in game (so both in-game and in-tools share the same parameters / functions) so I can set up the location, rotation etc. precisely. This would come with another feature: sitting and lying locations: for example a couch, or a 2-persons bed, can have different sitting locations, we should be able to set them in the tool (and add as many we want) and all characters (player or NPC) would choose between one of the available locations following the conditions: the closest AVAILABLE. If another character is already using a location, this one is unavlaible for other, makes sense.
Then, I want an event manager: completing a quest, using an asset etc can trigger events, this is a way to cleanup the tools as well and ensure a cleaner code where a leak, a fire, a phone call etc. becomes an event that can be called through different ways, we design an event (e.g. it will trigger a notification, have an impact on skills, needs, social, work, visa, etc. everything should be customizable in the event manager) and this event can be called whether by an interaction, an asset, a quest... FOR THIS ONE, DO NOT CODE IT YET BUT THINK OF A PLAN AND IF IT MAKES SENSE COMPARED TO THE CURRENT CODE, I DON'T WANT TO BREAK THE GAME!!
